---
phase: 03-unified-qa-status-view
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - public/index.html
autonomous: true
requirements:
  - DASH-05
user_setup: []

must_haves:
  truths:
    - "El `<select id=\"trendClientSelector\">` está populado con la unión de `clients[slug].name` observados en los últimos 30 runs (allRuns), ordenados alfabéticamente, con opción default `\"\"` = 'Todos los clientes (agregado)'"
    - "Durante el lazy-load inicial (1-2s), el dropdown muestra un placeholder `<option disabled>Cargando clientes…</option>` además de la opción default — reemplazado por la lista real cuando termina el fetch"
    - "Al seleccionar `\"\"`, el trend chart renderiza el agregado histórico (dos líneas Passed/Failed totales) — comportamiento idéntico a pre-Phase 3, pixel por pixel"
    - "Al seleccionar un `{slug}`, el trend chart re-renderiza con series `allRunsDetailed.map(r => r.clients?.[slug]?.passed ?? 0)` y `.failed ?? 0`, borderColor accent `#667eea` para Passed"
    - "Chart instance existente se destruye con `Chart.getChart(canvas)?.destroy()` antes de cada `new Chart(...)` — previene leak/overlay de Chart.js v4 (Pitfall 1)"
    - "Lazy load de `allRunsDetailed` (y su Promise cache `allRunsDetailedPromise`) definidos DESDE TASK 1 en versión race-safe — no hay cascada/sobrescritura en Task 3"
    - "Cuando el cliente seleccionado no tiene data en ningún run, `chartTitle` muestra 'Sin datos para {ClientName}' y canvas vacío (no error JS)"
    - "Default `\"\"` (Todos los clientes) está seleccionado al cargar; el dropdown NO se re-popula al cambiar `#runSelector` (D-07)"
  artifacts:
    - path: "public/index.html"
      provides: "populateTrendClientSelector (con placeholder de loading) + loadAllRunDetails race-safe + allRunsDetailed + allRunsDetailedPromise + updateTrendChart extendido con filterSlug + Chart.getChart destroy pattern + initDashboard wiring"
      contains_js: ["function populateTrendClientSelector", "function loadAllRunDetails", "let allRunsDetailed", "let allRunsDetailedPromise", "Chart.getChart(", ".destroy()", "filterSlug = null", "async function updateTrendChart(filterSlug", "trendClientSelector", "Cargando clientes"]
  key_links:
    - from: "initDashboard()"
      to: "populateTrendClientSelector() + addEventListener sobre #trendClientSelector"
      via: "Direct calls después de updateTrendChart()"
      pattern: "populateTrendClientSelector\\(\\)"
    - from: "#trendClientSelector change event"
      to: "updateTrendChart(e.target.value || null)"
      via: "addEventListener('change', ...) con async handler"
      pattern: "updateTrendChart\\(.*trendClientSelector"
    - from: "updateTrendChart(filterSlug)"
      to: "Chart.getChart(canvas)?.destroy() antes de new Chart"
      via: "Chart.js v4 API"
      pattern: "Chart\\.getChart\\(.*\\)\\?\\.destroy\\(\\)"
    - from: "updateTrendChart(slug)"
      to: "loadAllRunDetails() -> allRunsDetailed -> r.clients[slug]"
      via: "Lazy load Promise cached en allRunsDetailedPromise"
      pattern: "loadAllRunDetails\\(\\)"
---

<objective>
Cerrar DASH-05 agregando selector de cliente al trend chart. Popular el `<select id="trendClientSelector">` con la unión de clientes observados en los últimos 30 runs (D-04). Extender `updateTrendChart(filterSlug = null)` con: (a) path agregado preservado pixel-por-pixel cuando `filterSlug === null`, (b) path filtrado por cliente que lee `allRunsDetailed`, y (c) destroy/recreate pattern con `Chart.getChart(canvas)?.destroy()` para prevenir el bug v4 "Canvas is already in use". Lazy-load de 30 runs detallados al primer filter (D-04 + Open Question #1 RESOLVED), con Promise cacheada para race-safety.

Purpose: Cumple la pata final de Phase 3 — permite a Eduardo ver la curva histórica por cliente individual (ej "¿cómo viene Codelpa estos 30 días?") sin perder el agregado actual. La regresión-cero es crítica: el comportamiento "Todos los clientes" DEBE ser idéntico al `updateTrendChart()` actual.

Output: 3 funciones JS nuevas (`populateTrendClientSelector`, `loadAllRunDetails`, `wireTrendClientListener`) + 2 variables de cache (`allRunsDetailed`, `allRunsDetailedPromise`) + refactor de `updateTrendChart` a `async updateTrendChart(filterSlug = null)` + 2 invocaciones nuevas en `initDashboard`. Total ~80 líneas agregadas + ~26 líneas reemplazadas en updateTrendChart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-unified-qa-status-view/03-CONTEXT.md
@.planning/phases/03-unified-qa-status-view/03-RESEARCH.md
@.planning/phases/03-unified-qa-status-view/03-UI-SPEC.md
@.planning/phases/03-unified-qa-status-view/03-01-PLAN.md
@.planning/phases/03-unified-qa-status-view/03-02-PLAN.md
@public/index.html
@public/history/index.json

<interfaces>
<!-- Existing code the executor must integrate with -->

Current updateTrendChart (public/index.html lines 1976-2036 aprox):
- Lines 1976-2001: line chart (passed/failed aggregate) — THIS is what gets refactored.
- Lines 2003-2036: donut chart (distribution of latestRun) — PRESERVED UNCHANGED.

```javascript
async function updateTrendChart() {
    const labels = allRuns.map(r => r.date).reverse();
    const passedData = allRuns.map(r => r.passed).reverse();
    const failedData = allRuns.map(r => r.failed).reverse();

    document.getElementById('chartTitle').textContent =
        `${labels.length} run${labels.length !== 1 ? 's' : ''} · últimos 30 días`;

    new Chart(document.getElementById('trendChart').getContext('2d'), {
        type: 'line',
        data: {
            labels,
            datasets: [
                { label: 'Passed', data: passedData, borderColor: '#10b981', backgroundColor: 'rgba(16,185,129,0.06)', borderWidth: 2.5, fill: true, tension: 0.35, pointRadius: 5, pointBackgroundColor: '#10b981', pointBorderColor: '#fff', pointBorderWidth: 2 },
                { label: 'Failed',  data: failedData,  borderColor: '#ef4444', backgroundColor: 'rgba(239,68,68,0.06)',  borderWidth: 2.5, fill: true, tension: 0.35, pointRadius: 5, pointBackgroundColor: '#ef4444', pointBorderColor: '#fff', pointBorderWidth: 2 }
            ]
        },
        options: {
            responsive: true, maintainAspectRatio: false,
            plugins: { legend: { display: true, position: 'top', labels: { font: { size: 13, weight: 'bold' }, padding: 14, usePointStyle: true } } },
            scales: {
                y: { beginAtZero: true, grid: { drawBorder: false }, ticks: { stepSize: 10 } },
                x: { grid: { display: false } }
            }
        }
    });

    // Distribution donut (large)   <-- NO TOCAR — sigue siendo parte de updateTrendChart
    const { passed, failed } = latestRun;
    // ... donut chart code (líneas 2003-2036) NO SE MODIFICA
}
```

**IMPORTANTE:** `updateTrendChart` actualmente contiene DOS charts: el line trend + el donut. Sólo el LINE se modifica para soportar filterSlug. El donut queda intacto y siempre lee de `latestRun`. Las líneas exactas: el bloque a reemplazar va desde la firma (línea 1976 aprox) hasta el `});` de cierre del `new Chart(...)` del line chart (línea 2001 aprox), ANTES del comentario `// Distribution donut (large)` (línea 2003). El bloque del donut (líneas 2003-2036) incluyendo el `}` final de cierre de la función queda intocado.

Existing globals:
```javascript
let allRuns = [];  // populated by loadHistoryIndex(), contains {date, passed, failed, ...} per run
// NO tiene clients[slug] — eso está en history/{date}.json completo (loadRunDetails)

async function loadRunDetails(date) {
    // fetch(`history/${date}.json`) → full run incl. clients{}
}
```

Skeleton from Plan 01 (required):
- `<select id="trendClientSelector" class="run-select"></select>` — exists, empty

Patterns from Plan 02 (helpers available):
- `escapeHtml(str)` — for option labels
- `daysDiff` — available but not needed here
- `cachedManifest` pattern — model for `allRunsDetailed` lazy cache
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: populateTrendClientSelector (con placeholder UX) + loadAllRunDetails race-safe lazy cache</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html líneas 1470-1500 (loadHistoryIndex, initDashboard)
    - public/index.html línea 1412-1413 (globales latestRun/allRuns)
    - public/index.html líneas 1976-2001 (updateTrendChart actual — para entender estructura existente)
    - .planning/phases/03-unified-qa-status-view/03-UI-SPEC.md (Component 4 — Selector de cliente del trend chart)
    - .planning/phases/03-unified-qa-status-view/03-CONTEXT.md (D-04, D-06, D-07)
    - .planning/phases/03-unified-qa-status-view/03-RESEARCH.md (Open Question #1 RESOLVED — lazy load pattern)
  </read_first>
  <action>
  **Paso 1 — Agregar variables de cache + función `loadAllRunDetails` RACE-SAFE en el bloque `<script>`, cerca de donde Plan 02 agregó `let cachedManifest`.**

  **Importante:** `loadAllRunDetails` se declara DESDE AQUÍ en su versión final race-safe. Task 3 NO la sobrescribe. Esto evita la cascada de refactor entre tasks.

  Insertar después del bloque de `cachedManifest` / `loadManifestCached` (agregado por Plan 02):

  ```javascript
  // ── Phase 3 DASH-05: Per-client trend chart ──
  let allRunsDetailed = null;          // lazy cache: array of full run objects with clients{}
  let allRunsDetailedPromise = null;   // in-flight cache: previene race condition entre llamadas concurrentes

  async function loadAllRunDetails() {
      if (allRunsDetailed) return allRunsDetailed;
      if (allRunsDetailedPromise) return allRunsDetailedPromise;
      if (!Array.isArray(allRuns) || allRuns.length === 0) return [];
      allRunsDetailedPromise = Promise.all(
          allRuns.map(r => loadRunDetails(r.date).catch(() => null))
      ).then(results => {
          allRunsDetailed = results.filter(Boolean);
          return allRunsDetailed;
      });
      return allRunsDetailedPromise;
  }

  async function populateTrendClientSelector() {
      const sel = document.getElementById('trendClientSelector');
      if (!sel) return;

      // W5 mitigation: mostrar placeholder de loading mientras el lazy-load está en vuelo.
      // Durante 1-2s el usuario ve "Cargando clientes…" disabled en vez de un dropdown vacío.
      sel.innerHTML = [
          '<option value="" selected>Todos los clientes (agregado)</option>',
          '<option disabled>Cargando clientes…</option>'
      ].join('');

      // D-04: union de clients[slug].name observados en los últimos 30 runs (allRuns)
      const detailed = await loadAllRunDetails();
      const clientMap = {};  // { slug: name }
      for (const run of detailed) {
          if (!run || !run.clients) continue;
          for (const [slug, c] of Object.entries(run.clients)) {
              if (!clientMap[slug]) clientMap[slug] = c.name || slug;
          }
      }
      const options = [
          '<option value="" selected>Todos los clientes (agregado)</option>',
          ...Object.entries(clientMap)
              .sort(([, a], [, b]) => a.localeCompare(b))  // D-04: alfabético por name
              .map(([slug, name]) => `<option value="${escapeHtml(slug)}">${escapeHtml(name)}</option>`)
      ];
      sel.innerHTML = options.join('');  // reemplaza el placeholder con la lista real
  }
  ```

  **Notas:**
  - `allRunsDetailedPromise` cachea la Promise misma — si hay 2 llamadas concurrentes a `loadAllRunDetails()` antes de que la primera complete, ambas reusan el mismo fetch. Sin race, sin double-fetch.
  - `populateTrendClientSelector` dispara el lazy load en init (acepta el costo porque el dropdown necesita estar poblado). Esto desvía de la recomendación estricta del RESEARCH Open Question #1 (que sugería lazy en primer filter), pero es necesario para poblar el dropdown con los nombres correctos. El segundo filter-change usa el mismo cache.
  - **W5 fix — Placeholder UX:** Antes del `await`, el dropdown se setea con la opción default + `<option disabled>Cargando clientes…</option>`. Durante los 1-2s del lazy load, el usuario ve el estado de carga explícito. Cuando termina, `sel.innerHTML = options.join('')` reemplaza todo con la lista real. El usuario que intente filtrar antes de que cargue ve un dropdown funcional (puede elegir "Todos") pero con señal visual clara de que hay más por venir.
  - Alternativa descartada: poblar el dropdown solo con slugs de `allRuns` (que no tiene `clients`) — no funciona porque allRuns solo tiene `{date, passed, failed}` a nivel run, no por cliente.
  - `escapeHtml` en slug y name defensivo (XSS).
  - D-07: `populateTrendClientSelector` se llama UNA VEZ en `initDashboard` — no se re-llama al cambiar `#runSelector`.
  - Fallback: si `loadAllRunDetails()` retorna `[]`, `clientMap` queda vacío y el dropdown solo muestra la opción default "Todos los clientes (agregado)" — comportamiento degradado aceptable. El placeholder "Cargando clientes…" desaparece al reemplazar innerHTML.
  </action>
  <verify>
    <automated>
    grep -c "let allRunsDetailed = null;" public/index.html            # == 1
    grep -c "let allRunsDetailedPromise = null;" public/index.html     # == 1
    grep -c "async function loadAllRunDetails" public/index.html       # == 1 (DESDE Task 1 en versión race-safe — no sobrescrita en Task 3)
    grep -c "async function populateTrendClientSelector" public/index.html # == 1
    grep -c "Todos los clientes (agregado)" public/index.html          # >= 2 (placeholder + lista real usan el mismo string)
    grep -c "Cargando clientes" public/index.html                      # == 1 (W5 placeholder)
    grep -c "allRunsDetailedPromise" public/index.html                 # >= 3 (declaration + 2 refs dentro de loadAllRunDetails)
    grep -c "Promise\.all" public/index.html                           # >= 1 (puede haber otros usos)
    # Parse check
    python3 -c "
    with open('public/index.html') as f: html = f.read()
    assert 'function loadAllRunDetails' in html
    assert 'allRunsDetailed' in html
    assert 'allRunsDetailedPromise' in html
    assert 'Cargando clientes' in html
    print('OK')
    "
    </automated>
  </verify>
  <acceptance_criteria>
    - [x] `let allRunsDetailed = null;` declarado una vez en el script (ubicado cerca de `cachedManifest` de Plan 02).
    - [x] `let allRunsDetailedPromise = null;` declarado una vez, adyacente a `allRunsDetailed` — cache de Promise in-flight para race-safety.
    - [x] `async function loadAllRunDetails()` implementa versión FINAL race-safe desde Task 1 (no se sobrescribe en Task 3): early return si `allRunsDetailed` existe, early return si `allRunsDetailedPromise` existe, guard array, `allRunsDetailedPromise = Promise.all(...).then(results => { allRunsDetailed = results.filter(Boolean); return allRunsDetailed; })`, return `allRunsDetailedPromise`.
    - [x] `async function populateTrendClientSelector()` implementa: early return si `#trendClientSelector` no existe, setea `sel.innerHTML` con placeholder `<option disabled>Cargando clientes…</option>` ANTES del await, await `loadAllRunDetails()`, build `clientMap` como unión de `clients[slug].name`, sort alfabético por name, reemplaza `sel.innerHTML` con opción default + lista real.
    - [x] Opción default tiene atributo `selected` hardcoded (D-06) — presente tanto en el placeholder como en la lista final.
    - [x] Placeholder "Cargando clientes…" presente en el DOM durante el lazy load — verificable con `grep -c "Cargando clientes"` == 1.
    - [x] Options por cliente usan `<option value="${escapeHtml(slug)}">${escapeHtml(name)}</option>` — XSS-safe.
    - [x] Al abrir el dashboard en browser: `#trendClientSelector` muestra inicialmente "Todos los clientes (agregado)" + "Cargando clientes…" (disabled); tras 1-2s se reemplaza con una opción por cada cliente observado en los últimos 30 runs, alfabético.
    - [x] Console: `document.getElementById('trendClientSelector').options.length >= 2` después del lazy load.
    - [x] Las opciones aparecen con `c.name` (ej "Codelpa", "Sonrie (staging)"), NO con slugs.
  </acceptance_criteria>
  <done>
    Cache lazy (con Promise race-safety) y populador del selector (con placeholder UX durante lazy load) definidos en versión final. `populateTrendClientSelector` se invocará desde `initDashboard` en Task 3. La variable `allRunsDetailed` y su `allRunsDetailedPromise` quedan disponibles para reutilización en `updateTrendChart` (Task 2). Task 3 NO necesita modificar `loadAllRunDetails`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor updateTrendChart(filterSlug) con Chart.getChart destroy + per-client series</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html líneas 1976-2036 (updateTrendChart completo: line chart + donut — identificar fronteras exactas)
    - .planning/phases/03-unified-qa-status-view/03-UI-SPEC.md (Component 4 interaction + Fuentes decisión)
    - .planning/phases/03-unified-qa-status-view/03-RESEARCH.md (Pattern 1: Destroy + Recreate, Pitfall 1, Code Examples "Extend updateTrendChart with filterSlug")
    - .planning/phases/03-unified-qa-status-view/03-CONTEXT.md (D-04, D-05, D-06)
  </read_first>
  <action>
  **Contexto:** La función actual `updateTrendChart()` (líneas 1976-2036 aprox) contiene DOS charts en el mismo cuerpo de función:
  1. Líneas 1976-2001: line chart (aggregate Passed/Failed).
  2. Líneas 2003-2036: donut chart + `const { passed, failed } = latestRun;` + el `}` final que cierra la función.

  **Regla crítica de fronteras del refactor:**
  - **REEMPLAZAR** las líneas 1976-2001 — desde la línea de firma `async function updateTrendChart() {` hasta (e incluyendo) el `});` que cierra el `new Chart(...)` del line chart, incluyendo la línea en blanco justo antes del comentario `// Distribution donut (large)`.
  - **PRESERVAR** las líneas 2003-2036 intactas — el comentario `// Distribution donut (large)`, el bloque `const { passed, failed } = latestRun;`, el `new Chart(document.getElementById('donutChart')...)` completo, y el `}` final que cierra la función `updateTrendChart`.
  - La llave de cierre `}` de la función queda en la línea 2036 (aprox) — el refactor NO toca ese `}`.

  **Paso 1 — Reemplazar ÚNICAMENTE las líneas 1976-2001 con el siguiente bloque.** El donut (líneas 2003-2036) queda tal cual, incluyendo su `}` de cierre de función.

  Bloque de reemplazo (ocupa líneas ~1976-2002 post-edit, misma cantidad aprox de líneas):

  ```javascript
  async function updateTrendChart(filterSlug = null) {
      const canvas = document.getElementById('trendChart');
      // Pitfall 1: destroy existing chart instance before creating a new one (Chart.js v4 API)
      const existing = Chart.getChart(canvas);
      if (existing) existing.destroy();

      const labels = allRuns.map(r => r.date).reverse();
      let passedData, failedData, title, passedColor;

      if (!filterSlug) {
          // AGGREGATE PATH — must be pixel-identical to pre-Phase 3 behavior
          passedData = allRuns.map(r => r.passed).reverse();
          failedData = allRuns.map(r => r.failed).reverse();
          title = `${labels.length} run${labels.length !== 1 ? 's' : ''} · últimos 30 días`;
          passedColor = '#10b981';
      } else {
          // PER-CLIENT PATH — lazy-load allRunsDetailed, map to per-client series
          const detailed = await loadAllRunDetails();
          // Align detailed array to allRuns order (both sorted desc by date from loadHistoryIndex)
          // Fallback to 0 when a run has no entry for this slug (D-05: nullish coalesce)
          const detailedByDate = {};
          for (const run of detailed) {
              if (run && run.date) detailedByDate[run.date] = run;
          }
          passedData = allRuns.map(r => detailedByDate[r.date]?.clients?.[filterSlug]?.passed ?? 0).reverse();
          failedData = allRuns.map(r => detailedByDate[r.date]?.clients?.[filterSlug]?.failed ?? 0).reverse();
          // Derive client name from any run that has it
          let clientName = filterSlug;
          for (const run of detailed) {
              if (run?.clients?.[filterSlug]?.name) { clientName = run.clients[filterSlug].name; break; }
          }
          const hasAnyData = passedData.some(v => v > 0) || failedData.some(v => v > 0);
          title = hasAnyData
              ? `${clientName} · ${labels.length} runs · últimos 30 días`
              : `Sin datos para ${clientName}`;
          passedColor = '#667eea';  // accent when filtered (UI-SPEC Dimension 3)
      }

      document.getElementById('chartTitle').textContent = title;

      new Chart(canvas.getContext('2d'), {
          type: 'line',
          data: {
              labels,
              datasets: [
                  { label: 'Passed', data: passedData, borderColor: passedColor, backgroundColor: filterSlug ? 'rgba(102,126,234,0.06)' : 'rgba(16,185,129,0.06)', borderWidth: 2.5, fill: true, tension: 0.35, pointRadius: 5, pointBackgroundColor: passedColor, pointBorderColor: '#fff', pointBorderWidth: 2 },
                  { label: 'Failed',  data: failedData,  borderColor: '#ef4444', backgroundColor: 'rgba(239,68,68,0.06)', borderWidth: 2.5, fill: true, tension: 0.35, pointRadius: 5, pointBackgroundColor: '#ef4444', pointBorderColor: '#fff', pointBorderWidth: 2 }
              ]
          },
          options: {
              responsive: true, maintainAspectRatio: false,
              plugins: { legend: { display: true, position: 'top', labels: { font: { size: 13, weight: 'bold' }, padding: 14, usePointStyle: true } } },
              scales: {
                  y: { beginAtZero: true, grid: { drawBorder: false }, ticks: { stepSize: 10 } },
                  x: { grid: { display: false } }
              }
          }
      });
  ```

  **Crítico:** El bloque de reemplazo NO incluye un `}` final — el `}` que cierra la función `updateTrendChart` ya existe en la línea 2036 (DESPUÉS del bloque del donut que queda intacto). El bloque de reemplazo termina con `});` del `new Chart(...)` del line chart.

  **Paso 2 — Verificar las fronteras del refactor antes de commitear.**

  Tras el reemplazo, la estructura debe quedar así (conceptualmente):
  ```
  línea 1976:    async function updateTrendChart(filterSlug = null) {
  líneas 1977-2001:   [nuevo cuerpo del line chart: Chart.getChart destroy, if/else, new Chart line]
  línea 2002:    (blank line — opcional, preservada)
  línea 2003:        // Distribution donut (large)           ← SIN MODIFICAR
  líneas 2004-2035:  [donut chart block SIN MODIFICAR]
  línea 2036:    }                                           ← SIN MODIFICAR (cierre de función)
  ```

  Grep sanity:
  - `grep -c "^async function updateTrendChart" public/index.html` debe ser `1` (una sola declaración de la función).
  - `grep -c "new Chart(document.getElementById('donutChart')" public/index.html` debe ser `1` (donut intacto).
  - `grep -c "const { passed, failed } = latestRun;" public/index.html` debe ser `1` (destructuring del donut intacto).

  **Destroy pattern para el donut (opcional pero NO requerido):** Al refactorizar, el donut puede también beneficiarse del mismo pattern si en el futuro se re-renderiza. Pero en la implementación actual del codebase, `updateTrendChart` solo se llama una vez en `initDashboard` — el donut solo se construye una vez. Por defecto, NO agregar destroy al donut en este plan (scope mínimo).

  **Consideración performance:** La primera llamada a `updateTrendChart(slug)` con un slug no vacío dispara `loadAllRunDetails()` que fetchea hasta 30 archivos en paralelo (~1-2s en red rápida). Durante esa espera, el chart muestra el estado previo (aggregate). Accept this lag como trade-off de scope — deferred optimization es pre-cachear todos los runs en init.

  **Compatibilidad con Phase 2 runSelector:**
  - El listener de Phase 2 NO llama a `updateTrendChart` (solo `updateClients`).
  - El listener de Phase 3 Plan 02 NO llama a `updateTrendChart` (solo `updateUnifiedQaTable`).
  - Por tanto `updateTrendChart` solo se re-invoca al cambiar `#trendClientSelector` (Task 3).
  - Esto preserva D-05 de Phase 2 (run selector NO sincroniza con trend chart — "Phase 3 explícitamente fuera de scope").
  </action>
  <verify>
    <automated>
    grep -c "^async function updateTrendChart" public/index.html      # == 1 (UNA SOLA declaración de la función)
    grep -c "async function updateTrendChart(filterSlug = null)" public/index.html  # == 1
    grep -c "Chart\.getChart(" public/index.html               # >= 1
    grep -c "existing\.destroy()" public/index.html            # >= 1
    grep -c "loadAllRunDetails()" public/index.html            # >= 2 (declaration + call en updateTrendChart)
    grep -c "Sin datos para " public/index.html                # == 1
    grep -c "últimos 30 días" public/index.html                # >= 2 (agregado + per-client — ambos templates)
    # Verificar que el donut chart NO se tocó
    grep -c "new Chart(document.getElementById('donutChart')" public/index.html  # == 1 (intacto)
    grep -c "const { passed, failed } = latestRun;" public/index.html            # == 1 (destructuring intacto)
    grep -c "donutChart" public/index.html                                       # >= 1
    # Verificar que la función cierra correctamente (línea con `}` DESPUÉS del donut)
    python3 -c "
    with open('public/index.html') as f: text = f.read()
    # Debe haber exactamente una declaración de updateTrendChart
    import re
    decls = re.findall(r'^async function updateTrendChart', text, re.MULTILINE)
    assert len(decls) == 1, f'Expected 1 declaration, got {len(decls)}'
    # El donut debe estar DESPUÉS del line chart dentro de la misma función
    idx_fn = text.index('async function updateTrendChart')
    idx_line = text.index('new Chart(canvas.getContext', idx_fn)
    idx_donut = text.index('new Chart(document.getElementById(\\'donutChart\\')', idx_fn)
    assert idx_line < idx_donut, 'Line chart must come before donut inside updateTrendChart'
    print('Structure OK')
    "
    # Browser smoke test:
    # 1. Cargar dashboard: trend chart se renderiza igual que antes (agregado)
    # 2. En console: Chart.getChart(document.getElementById('trendChart')) returns Chart instance
    # 3. En console: await updateTrendChart('codelpa'); → chart cambia a serie per-cliente, color Passed = #667eea
    # 4. await updateTrendChart();  → chart vuelve al agregado, color Passed = #10b981
    # 5. await updateTrendChart('cliente_inexistente'); → chartTitle = "Sin datos para cliente_inexistente", líneas en 0
    # 6. Donut sigue renderizando distribución del latestRun
    open public/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - [x] Firma `async function updateTrendChart(filterSlug = null)` — parámetro opcional con default `null`. `grep -c "^async function updateTrendChart" public/index.html` == 1 (única declaración).
    - [x] El REEMPLAZO cubrió SOLO las líneas 1976-2001 (desde la firma hasta el `});` del line chart). Las líneas 2003-2036 (donut block + `}` final de la función) quedan intactas — verificable con `grep -c "const { passed, failed } = latestRun;"` == 1 y `grep -c "new Chart(document.getElementById('donutChart')"` == 1.
    - [x] `Chart.getChart(canvas)?.destroy()` pattern presente AL INICIO de la función (antes de cualquier `new Chart(...)`).
    - [x] Aggregate path (filterSlug null) produce `passedData` y `failedData` desde `allRuns.map(r => r.passed/r.failed).reverse()` — idéntico al código pre-Phase 3.
    - [x] Aggregate path usa `borderColor: '#10b981'` para Passed — idéntico al pre-Phase 3.
    - [x] Aggregate path title `${N} run{s} · últimos 30 días` — idéntico al pre-Phase 3.
    - [x] Per-client path llama `await loadAllRunDetails()`, construye un mapa `detailedByDate`, extrae `.clients?.[filterSlug]?.passed ?? 0` y `.failed ?? 0`.
    - [x] Per-client path usa `borderColor: '#667eea'` (accent) para Passed — UI-SPEC Dimension 3.
    - [x] Per-client path title formato `${clientName} · ${N} runs · últimos 30 días`.
    - [x] Per-client path sin-data: title `Sin datos para ${clientName}` y data arrays todos en 0.
    - [x] Donut chart block (`new Chart(document.getElementById('donutChart'))...`) NO MODIFICADO — sigue usando `latestRun.passed/failed`.
    - [x] `const { passed, failed } = latestRun;` (parte del donut) NO modificado.
    - [x] El `}` final de cierre de la función `updateTrendChart` (después del donut block) queda intacto en su posición original — la función no se rompe estructuralmente.
    - [x] Sin errors en browser console al cargar el dashboard.
    - [x] Al abrir dashboard, el trend chart se ve idéntico al antes (dos líneas verde/rojo, mismo título formato).
    - [x] Al abrir dashboard, el donut chart se ve idéntico al antes (distribución del latestRun).
    - [x] En browser console: llamar `await updateTrendChart('codelpa')` cambia el chart sin lanzar "Canvas is already in use" (Pitfall 1 verificado).
    - [x] En browser console: llamar `await updateTrendChart()` (sin args) restaura el agregado.
  </acceptance_criteria>
  <done>
    `updateTrendChart` acepta `filterSlug`, preserva el comportamiento agregado pixel-por-pixel, soporta filtro per-cliente con lazy load, y destruye la instancia anterior antes de crear una nueva (Chart.js v4 safe). El donut sigue intacto en su posición original dentro de la misma función. Falta solo wirearlo al `<select>` en Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: wireTrendClientListener + populateTrendClientSelector en initDashboard</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html líneas 1481-1503 post-Plan-02 (initDashboard con setupUnifiedFilterPills, updateUnifiedQaTable, wireUnifiedQaRunListener)
    - .planning/phases/03-unified-qa-status-view/03-CONTEXT.md (D-06, D-07)
    - .planning/phases/03-unified-qa-status-view/03-UI-SPEC.md (Interaction Contract: Re-render al cambiar cliente del trend)
  </read_first>
  <action>
  **Paso 1 — Agregar función `wireTrendClientListener` en el script, ubicada cerca de `wireUnifiedQaRunListener` (agregada por Plan 02).**

  ```javascript
  function wireTrendClientListener() {
      const sel = document.getElementById('trendClientSelector');
      if (!sel || sel.dataset.wired === 'true') return;
      sel.dataset.wired = 'true';
      sel.addEventListener('change', (e) => {
          const slug = e.target.value || null;  // "" → null (aggregate)
          updateTrendChart(slug);
      });
  }
  ```

  **Paso 2 — Modificar `initDashboard()` para invocar `populateTrendClientSelector()` y `wireTrendClientListener()` DESPUÉS de `updateTrendChart()`.**

  Estado actual (post-Plan-02, orden corregido por D-02):
  ```javascript
          updateClients();
          updateUnifiedQaTable(latestRun);
          setupUnifiedFilterPills();
          populateRunSelector();
          wireUnifiedQaRunListener();
          updateTrendChart();
  ```

  Nuevo estado:
  ```javascript
          updateClients();
          updateUnifiedQaTable(latestRun);
          setupUnifiedFilterPills();
          populateRunSelector();
          wireUnifiedQaRunListener();
          updateTrendChart();                            // existing — renderiza agregado inicial
          populateTrendClientSelector();                 // Phase 3 DASH-05: popula el dropdown (placeholder "Cargando clientes…" inmediato, luego lista tras 1-2s)
          wireTrendClientListener();                     // Phase 3 DASH-05: wire change handler
  ```

  **Notas críticas:**
  - **`loadAllRunDetails` NO se redefine aquí.** La versión race-safe con `allRunsDetailedPromise` ya está declarada en Task 1. Task 3 solo consume, no modifica.
  - `populateTrendClientSelector()` es async pero NO hacemos await — el placeholder "Cargando clientes…" (agregado en Task 1) se muestra INMEDIATAMENTE al setear `sel.innerHTML` antes del await interno, así que el usuario tiene feedback visual durante el lazy load de 1-2s. Cuando termina, el populate reemplaza el placeholder con la lista real.
  - Alternativa descartada: `await populateTrendClientSelector()` antes de `wireTrendClientListener()` — introduce lag de 1-2s en el render inicial del dashboard. Peor UX que tener el dropdown con placeholder visible.
  - `wireTrendClientListener()` NO await — el listener es sincrónico, solo registra.
  - Orden: `updateTrendChart()` primero (renderiza el chart inicial con agregado), luego `populateTrendClientSelector()` (setea placeholder + dispara lazy load + llena el dropdown en background), luego `wireTrendClientListener()` (listo para cambios del usuario — aunque el dropdown aún muestre "Cargando clientes…", el usuario puede elegir "Todos los clientes" sin problema).

  **Edge case — usuario cambia el dropdown antes de que termine el lazy load:**
  - Si elige "Todos los clientes" → `updateTrendChart(null)` → aggregate path, NO llama `loadAllRunDetails`. OK.
  - Si elige "Cargando clientes…" → imposible, el `<option disabled>` bloquea selección.
  - Si elige un slug real (solo posible DESPUÉS del replace del innerHTML) → `updateTrendChart(slug)` → llama `loadAllRunDetails()` que ya resolvió (cache hit) o la Promise está en vuelo (reusa `allRunsDetailedPromise`). Sin race.
  </action>
  <verify>
    <automated>
    grep -c "function wireTrendClientListener" public/index.html  # == 1
    grep -c "wireTrendClientListener();" public/index.html        # == 1 (invocación en initDashboard)
    grep -c "populateTrendClientSelector();" public/index.html    # == 1 (invocación en initDashboard)
    # Verificar que loadAllRunDetails NO fue redefinida en Task 3 (sigue siendo la versión race-safe de Task 1)
    grep -c "async function loadAllRunDetails" public/index.html  # == 1 (única declaración, desde Task 1)
    # Verificar orden en initDashboard
    python3 -c "
    with open('public/index.html') as f: text = f.read()
    start = text.index('async function initDashboard()')
    end = text.index('}', start + 200) + 1
    body = text[start:end]
    idx_chart = body.index('updateTrendChart();')
    idx_pop = body.index('populateTrendClientSelector();')
    idx_wire = body.index('wireTrendClientListener();')
    assert idx_chart < idx_pop < idx_wire, f'wrong order: chart={idx_chart}, pop={idx_pop}, wire={idx_wire}'
    print('Order OK')
    "
    # Race-safe cache variant present (declarada en Task 1)
    grep -c "allRunsDetailedPromise" public/index.html  # >= 3 (declaration + 2 refs dentro de loadAllRunDetails)
    # Browser smoke:
    # 1. Cargar dashboard: chart renderiza agregado, dropdown aparece con "Todos los clientes (agregado)" + "Cargando clientes…" (disabled)
    # 2. ~1-2s después: dropdown reemplaza "Cargando clientes…" con clientes observados
    # 3. Seleccionar "Codelpa": chart cambia a serie Codelpa con línea #667eea
    # 4. Seleccionar "Todos los clientes (agregado)": chart vuelve a agregado verde
    # 5. Múltiples cambios rápidos: sin errors, sin "Canvas is already in use"
    # 6. Phase 2 clients cards: siguen funcionando
    # 7. #runSelector: cambiar run NO re-puebla el trend dropdown (D-07), NO cambia el trend chart
    open public/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - [x] `function wireTrendClientListener()` existe con event delegation sobre `#trendClientSelector`, flag `dataset.wired`, convierte `""` a `null` antes de llamar `updateTrendChart`.
    - [x] `initDashboard()` invoca (en este orden): `updateTrendChart(); populateTrendClientSelector(); wireTrendClientListener();` — el chart se renderiza primero, dropdown se pobla después (con placeholder inmediato de Task 1).
    - [x] `populateTrendClientSelector()` NO se invoca desde ningún otro lugar (D-07 — se pobla una sola vez).
    - [x] `wireTrendClientListener()` NO se invoca desde ningún otro lugar (solo init).
    - [x] `loadAllRunDetails` NO se redefine en Task 3 — sigue siendo la versión race-safe declarada en Task 1. `grep -c "async function loadAllRunDetails"` == 1.
    - [x] Listener Phase 2 sobre `#runSelector` (populateRunSelector body) SIN MODIFICAR — no toca updateTrendChart.
    - [x] Listener Phase 3 Plan 02 sobre `#runSelector` (wireUnifiedQaRunListener) SIN MODIFICAR — no toca updateTrendChart.
    - [x] Browser smoke: al cargar, el usuario ve inmediatamente "Todos los clientes (agregado)" + "Cargando clientes…" (disabled) en el dropdown, y tras 1-2s la lista real de clientes reemplaza el placeholder.
    - [x] Browser smoke: al cambiar el `<select>` de cliente, el trend chart se re-renderiza sin error, con borderColor `#667eea` cuando se filtra y `#10b981` cuando se vuelve a "Todos".
    - [x] Browser smoke: al cambiar `#runSelector`, el trend dropdown NO cambia y el trend chart NO se re-renderiza (solo updateClients + updateUnifiedQaTable).
    - [x] Console: rápido clicking entre opciones del dropdown no genera "Canvas is already in use" (Chart.getChart destroy funciona).
  </acceptance_criteria>
  <done>
    DASH-05 completo. El trend chart tiene selector de cliente funcional, el dropdown muestra placeholder "Cargando clientes…" durante el lazy load y se pobla automáticamente con los clientes observados en los últimos 30 runs, y seleccionar un cliente re-renderiza el chart con su curva histórica. Seleccionar "Todos los clientes" restaura el agregado idéntico al pre-Phase 3. Cambiar de run NO afecta el trend chart (D-05 Phase 2 preservada). Phase 3 completa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Igual que Plan 02 — GitHub Pages CDN → Browser → DOM. Sin nuevas trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering (XSS) | `c.name` en `<option>` del trend dropdown | mitigate | `escapeHtml(name)` y `escapeHtml(slug)` aplicados en la construcción del option. |
| T-03-08 | Denial of service | Lazy load de 30 `history/*.json` archivos en Promise.all | accept | Cada archivo es <50KB = 1.5MB total máx. Tiempo 1-2s red rápida. Tolerable. |
| T-03-09 | Race condition | Concurrent `loadAllRunDetails()` calls antes del primer complete | mitigate | `allRunsDetailedPromise` (declarado en Task 1) cachea la Promise misma, la segunda llamada reusa la in-flight request. No hay refactor en Task 3. |
| T-03-10 | Regression | `updateTrendChart()` sin args debe comportarse idéntico al pre-Phase 3 | mitigate | Aggregate path usa los mismos `allRuns.map(r => r.passed)`, mismo color `#10b981`, mismo title format. Chart.getChart destroy solo agrega, no altera el output visual. Verificar en browser con diff visual antes/después. |
</threat_model>

<verification>
1. **Aggregate path sin regresión:** Al cargar el dashboard, el trend chart debe verse IDÉNTICO a antes de Phase 3:
   - Línea Passed en `#10b981` (verde).
   - Línea Failed en `#ef4444` (rojo).
   - Title "{N} runs · últimos 30 días".
   - Donut chart intacto con distribución del `latestRun`.
2. **Dropdown UX completo:** Al cargar el dashboard:
   - Inmediatamente: `<select id="trendClientSelector">` muestra "Todos los clientes (agregado)" (selected) + "Cargando clientes…" (disabled).
   - Tras 1-2s: el placeholder se reemplaza con una opción por cliente observado, alfabético.
3. **Per-client render:** Seleccionar un cliente → chart se re-renderiza con:
   - borderColor Passed: `#667eea` (accent).
   - Data arrays extraídos de `allRunsDetailed[i].clients[slug]`.
   - Title "{ClientName} · {N} runs · últimos 30 días".
4. **Empty state:** Seleccionar un slug ficticio (via devtools `sel.value='nonexistent'; sel.dispatchEvent(new Event('change'))`) → title "Sin datos para nonexistent" sin errors.
5. **Destroy pattern:** Múltiples cambios de selector sin error "Canvas is already in use".
6. **D-07 respected:** Cambiar `#runSelector` NO re-pobla ni re-renderiza el trend chart.
7. **No cross-interference:** Cambiar `#trendClientSelector` NO afecta la tabla unificada, client cards, o Cowork grid.
8. **Phase 2 + Plan 02 listeners intactos:** Dos listeners separados sobre `#runSelector`, ambos funcionan.
9. **No cascada de refactor:** `loadAllRunDetails` declarada una sola vez (en Task 1, versión race-safe). Task 3 no la reemplaza.
</verification>

<success_criteria>
- [x] DASH-05 satisfecho: trend chart tiene selector de cliente funcional que filtra la curva histórica por cliente.
- [x] Regresión-zero: "Todos los clientes" es pixel-idéntico al comportamiento pre-Phase 3.
- [x] Chart.js safe: destroy pattern previene leaks en re-renders múltiples.
- [x] Lazy load: `allRunsDetailed` se fetcha una sola vez (cached) al primer populateTrendClientSelector.
- [x] Race-safe desde Task 1: concurrent calls a `loadAllRunDetails` reusan la Promise in-flight vía `allRunsDetailedPromise`. Sin cascada de refactor en Task 3.
- [x] UX durante lazy load: placeholder "Cargando clientes…" visible mientras carga (W5 mitigation).
- [x] D-04: dropdown alfabético por `c.name`, unión de clientes en 30 runs.
- [x] D-05: clients sin data en un run → 0 (nullish coalesce).
- [x] D-06: widget `<select>`, default `""` = "Todos los clientes (agregado)".
- [x] D-07: NO re-popula al cambiar `#runSelector`.
- [x] Refactor boundaries explícitas en Task 2: líneas 1976-2001 reemplazadas, líneas 2003-2036 (donut + `}` final) preservadas.
- [x] XSS-safe: `escapeHtml` aplicado a slug y name en options.
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-qa-status-view/03-03-SUMMARY.md` describing:
- Refactor de `updateTrendChart` (firma nueva, aggregate vs filtered paths)
- Fronteras exactas del refactor: líneas 1976-2001 reemplazadas, donut (2003-2036) + `}` final preservados
- Chart.getChart destroy pattern (Pitfall 1 mitigation)
- Lazy load architecture (`allRunsDetailed` cache + `allRunsDetailedPromise` race-safety — declarados en Task 1, sin sobrescritura)
- W5 mitigation: placeholder "Cargando clientes…" durante lazy load
- Functions added: `populateTrendClientSelector`, `loadAllRunDetails`, `wireTrendClientListener`
- initDashboard wiring final (orden de invocaciones, respetando D-02 de Plan 02)
- Regression verification: aggregate path pixel-idéntico
- Verificación del donut NO modificado
- Phase 3 overall verification contra los 5 success criteria del ROADMAP

**IMPORTANTE — Cierre de Phase 3 (CLAUDE.md flujo "push a main, sin PRs"):**
Tras completar el SUMMARY de este plan, el executor DEBE hacer `git add` + `git commit` + `git push origin main` en un solo flujo para que los cambios se desplieguen a GitHub Pages. El dashboard vive en `public/index.html` servido por GitHub Pages — los cambios NO son visibles hasta que el push se complete. No abrir PRs; el usuario trabaja con push directo a main (regla global del usuario). Si el remoto rechaza, hacer `git pull --rebase origin main` y reintentar el push.
</output>
</output>
