---
phase: 06-actionable-reports-agent-precision
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-specs/.agents/playwright-specialist.md
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02

must_haves:
  truths:
    - "El playwright-specialist sabe exactamente qué señales definen un fallo como flaky, ambiente o bug"
    - "El playwright-specialist sabe cuándo reintentar un test y cuándo escalarlo sin ambigüedad"
    - "Dos instancias de Claude usando el mismo agente clasificarán el mismo fallo de la misma forma"
  artifacts:
    - path: "ai-specs/.agents/playwright-specialist.md"
      provides: "Protocolo de clasificación de errores + criterios de retry/escalate"
      contains: "## Error Classification"
  key_links:
    - from: "playwright-specialist.md ## Error Classification"
      to: "/triage-playwright command Step 4"
      via: "el specialist es el rol que usa triage-playwright para clasificar"
      pattern: "flaky.*ambiente.*bug"
    - from: "criterios retry/escalate"
      to: "decisión concreta por tipo de fallo"
      via: "regla Si X → Y"
      pattern: "Escalar si.*2.*veces"
---

<objective>
Agregar sección `## Error Classification` a `ai-specs/.agents/playwright-specialist.md` con criterios explícitos y no-ambiguos para clasificar fallos como `flaky`, `ambiente` o `bug`, y agregar protocolo `## Retry vs Escalate` con reglas de cuándo reintentar vs escalar.

Purpose: Actualmente el specialist no tiene criterios de clasificación propios — depende de lo que `publish-results.py` pre-clasifica en history.json. Con esta sección, Claude puede clasificar en tiempo real durante un triage sin necesidad de leer datos históricos. La consistencia entre sesiones mejora porque los criterios son reglas fijas, no juicio ad-hoc.

Output: `ai-specs/.agents/playwright-specialist.md` con dos secciones nuevas al final: `## Error Classification` (flaky/ambiente/bug con señales y acción) y `## Retry vs Escalate` (reglas If/Then concretas).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Estructura actual de playwright-specialist.md -->
<!-- Tiene sección ## Debugging failures como punto 5 de Key Responsibilities -->
<!-- Las nuevas secciones van AL FINAL del archivo, después de ## Key Documents -->
<!-- playwright-failure-analyst.md ya tiene tabla de categorías bug/ux/ambiente/flaky -->
<!-- Las nuevas secciones deben ser consistentes con esa tabla (no contradecirla) -->
<!-- Patrón del proyecto: reglas expresadas como "**Si X → Y**" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar sección ## Error Classification con criterios para flaky, ambiente y bug</name>
  <files>ai-specs/.agents/playwright-specialist.md</files>
  <read_first>
    - ai-specs/.agents/playwright-specialist.md — leer completo; verificar que no existe ya una sección de clasificación de errores
    - ai-specs/.agents/playwright-failure-analyst.md — leer la tabla de categorías (líneas ~17-25) para no contradecir definiciones existentes
    - .planning/phases/06-actionable-reports-agent-precision/06-CONTEXT.md — D-06 (definición de flaky: estimación visual, sin histórico)
  </read_first>
  <action>
Agregar al FINAL de `ai-specs/.agents/playwright-specialist.md`, después de la sección `## Key Documents`, el siguiente bloque exacto:

```markdown
## Error Classification

Cuando un test falla, clasificar usando estas señales antes de actuar. La clasificación no requiere leer `public/history/*.json` — observar el contexto del run actual es suficiente.

### Flaky
**Señales:**
- El test pasó en runs anteriores y falló hoy una vez
- Pasa si se corre en retry inmediato
- No hay assertion determinista rota (el elemento existe, la API respondió, pero el timing fue distinto)

**Umbral orientador:** ocurre en menos del 30% de los runs (estimación visual — no requiere calcular histórico).

**Acción:** monitorear en próximos runs. No crear ticket Linear. Proponer annotation `.info()` para tracking.

---

### Ambiente
**Señales:**
- Timeout de red o staging lento
- Error de conexión puntual
- El selector cambia entre deploys (staging recibió un cambio de UI)
- El mismo test pasa en otro cliente del mismo run

**Nota sobre duración del timeout (rúbrica):**
- `<5s` → selector issue (el elemento no existe o cambió — bug de UI o cambio de deploy)
- `5-30s` → red/staging (ambiente lento o caído)
- `>30s` → bug o infinite loop (lógica de la app, no infraestructura) — reclasificar como `bug`

**Acción:** ignorar para este run. Si es selector roto, parchear el spec (ver `## Debugging failures`). Verificar que staging esté disponible.

---

### Bug
**Señales:**
- Falla reproducible: mismo resultado en retry
- Assertion determinista rota (la app devuelve valor incorrecto o elemento ausente cuando debería existir)
- Afecta al menos 1 cliente de forma consistente
- No se explica por timing ni por staging lento

**Acción:** crear ticket Linear con título `[QA] {razón corta} — {cliente}`, screenshot si existe, run date.
```
  </action>
  <verify>
    <automated>grep -n "## Error Classification\|### Flaky\|### Ambiente\|### Bug" /Users/lalojimenez/qa/ai-specs/.agents/playwright-specialist.md</automated>
  </verify>
  <done>
    - `grep "## Error Classification"` retorna exactamente 1 línea
    - `grep "### Flaky\|### Ambiente\|### Bug"` retorna exactamente 3 líneas
    - `grep "estimación visual"` retorna la descripción del umbral de flaky
    - `grep "30% de los runs"` retorna la línea del umbral orientador
  </done>
</task>

<task type="auto">
  <name>Task 2: Agregar sección ## Retry vs Escalate con reglas If/Then explícitas</name>
  <files>ai-specs/.agents/playwright-specialist.md</files>
  <read_first>
    - ai-specs/.agents/playwright-specialist.md — confirmar estado tras Task 1; localizar el final del archivo para agregar después de ## Error Classification
    - .planning/phases/06-actionable-reports-agent-precision/06-CONTEXT.md — D-07 (reintentar si timeout aislado, escalar si 2+ veces seguidas o P0)
  </read_first>
  <action>
Agregar DESPUÉS de la sección `## Error Classification` (al final del archivo) el siguiente bloque exacto:

```markdown
## Retry vs Escalate

Reglas para decidir si reintentar un test fallido o escalarlo inmediatamente.

**Reintentar si:**
- El fallo es un timeout aislado (falló 1 vez, no hubo retry previo, o pasó en retry inmediato)
- El error es de red puntual (connection reset, staging 502, DNS timeout)
- La categoría preliminar es `flaky` o `ambiente` por timing

**Escalar si:**
- El mismo assertion falla 2 o más veces seguidas (mismo test, mismo cliente, misma sesión)
- El fallo es P0: auth roto (no puede hacer login), checkout imposible, datos de cliente incorrectos
- La categoría es `bug` con screenshot que confirma el estado incorrecto

**Protocolo de retry:**
1. Correr el test fallido una vez más: `npx playwright test --grep "{test name}" --project=b2b`
2. Si pasa → clasificar como `flaky`, agregar a monitoreo
3. Si falla de nuevo con el mismo assertion → clasificar como `bug`, escalar con ticket Linear

**Regla P0:** Un fallo P0 nunca se retryea en silencio — siempre escalar inmediatamente al iniciar el triage.
```
  </action>
  <verify>
    <automated>grep -n "## Retry vs Escalate\|Reintentar si\|Escalar si\|Protocolo de retry" /Users/lalojimenez/qa/ai-specs/.agents/playwright-specialist.md</automated>
  </verify>
  <done>
    - `grep "## Retry vs Escalate"` retorna exactamente 1 línea
    - `grep "2 o más veces seguidas"` retorna la regla de escalamiento por assertion repetido
    - `grep "Protocolo de retry"` retorna la sección con los 3 pasos
    - `grep "Regla P0"` retorna la regla de P0 sin retry silencioso
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → ai-specs/.agents/playwright-specialist.md | Modificación de instrucciones que Claude adopta como rol durante triage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | playwright-specialist.md | accept | Solo texto Markdown — las instrucciones son leídas, no ejecutadas. Worst case: clasificación incorrecta que se corrige en el siguiente triage. |
| T-06-04 | Repudiation | Error Classification | accept | Las clasificaciones quedan en triage-{date}.md committeado — hay traza de decisiones. |
</threat_model>

<verification>
Después de ejecutar ambas tareas:

```bash
# 1. Verificar estructura completa de las dos secciones nuevas
grep -n "## Error Classification\|## Retry vs Escalate\|### Flaky\|### Ambiente\|### Bug\|Reintentar si\|Escalar si" \
  /Users/lalojimenez/qa/ai-specs/.agents/playwright-specialist.md

# 2. Verificar que la rúbrica de timeout está en el specialist (consistencia con triage-playwright)
grep -n "5s\|30s\|selector issue\|red/staging" \
  /Users/lalojimenez/qa/ai-specs/.agents/playwright-specialist.md

# 3. Verificar que el archivo no se rompió — debe seguir teniendo las secciones originales
grep -n "## Key Responsibilities\|## Writing E2E Specs\|## Key Documents" \
  /Users/lalojimenez/qa/ai-specs/.agents/playwright-specialist.md
```
</verification>

<success_criteria>
1. `ai-specs/.agents/playwright-specialist.md` contiene sección `## Error Classification` con subsecciones ### Flaky, ### Ambiente, ### Bug
2. Cada subsección tiene: señales observables, acción a tomar, y es consistente con las definiciones de `playwright-failure-analyst.md`
3. La definición de flaky incluye "estimación visual — no requiere calcular histórico" (per D-06)
4. La rúbrica de timeout (`<5s`, `5-30s`, `>30s`) aparece en la subsección ### Ambiente
5. Existe sección `## Retry vs Escalate` con reglas If/Then explícitas
6. La regla de escalar incluye "2 o más veces seguidas" (per D-07)
7. La regla P0 (escalar inmediatamente, sin retry silencioso) está documentada
</success_criteria>

<output>
Después de completar, crear `.planning/phases/06-actionable-reports-agent-precision/06-02-SUMMARY.md`
</output>
