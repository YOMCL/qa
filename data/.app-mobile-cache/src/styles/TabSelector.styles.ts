import { StyleSheet } from 'react-native';

export const tabSelectorStyles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    marginTop: 16,
    marginBottom: 24,
  },
  tabButton: {
    borderWidth: 1,
  },
});
