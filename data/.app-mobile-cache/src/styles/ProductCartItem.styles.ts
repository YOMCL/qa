import { StyleSheet } from 'react-native';

export const productCartItemStyles = StyleSheet.create({
  container: {
    marginBottom: 8,
  },
  subtotalContainer: {
    backgroundColor: '#FFFFFF',
    padding: 12,
    marginTop: -8,
    borderBottomLeftRadius: 20,
    borderBottomRightRadius: 20,
    borderWidth: 1,
    borderColor: '#F0F0F0',
    borderTopWidth: 0,
  },
}); 