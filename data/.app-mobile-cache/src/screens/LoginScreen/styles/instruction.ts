import { StyleSheet } from 'react-native';

export const instruction = StyleSheet.create({
  instructionText: {
    fontSize: 16,
    color: '#666666',
    textAlign: 'center',
    marginBottom: 32,
    lineHeight: 22,
  },
}); 