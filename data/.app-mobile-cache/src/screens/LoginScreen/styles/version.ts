import { StyleSheet } from 'react-native';

export const version = StyleSheet.create({
  versionText: {
    color: '#999999',
    fontSize: 12,
    textAlign: 'center',
    marginTop: 24,
    position: 'absolute',
    bottom: 24,
  },
}); 