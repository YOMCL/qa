import { StyleSheet } from 'react-native';

export const microsoftButton = StyleSheet.create({
  button: {
    marginTop: 20,
    marginBottom: 10,
  },
  label: {
    fontSize: 18,
    fontWeight: '600',
  },
}); 