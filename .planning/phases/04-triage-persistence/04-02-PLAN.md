---
phase: 04-triage-persistence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-specs/.commands/triage-playwright.md
autonomous: true
requirements:
  - PROC-01
tags:
  - claude-command
  - triage
  - persistence
  - git-flow
must_haves:
  truths:
    - "Al completar un triage, /triage-playwright escribe QA/{CLIENT}/{DATE}/triage-{date}.md con una sección ## por failure_group clasificado"
    - "El archivo tiene YAML frontmatter (client, date, total_failures, triaged_count, triaged_by) + una sección ## por failure_group"
    - "Cada sección tiene un bloque fenced ```yaml con reason_match, category (bug|flaky|ambiente), rationale, linear_ticket, action_taken"
    - "Si un failure_group afecta múltiples clientes, la misma entrada aparece en el triage-{date}.md de cada cliente (D-08)"
    - "Comando termina con git add + commit + push automático con mensaje chore(triage): {CLIENTES} {FECHA} — {N} failure_groups classified"
    - "Push falla y recupera con pull --rebase + retry (CLAUDE.md global)"
    - "reason_match usa comillas dobles YAML para permitir escaping correcto de backticks/backslashes/dollar signs"
  artifacts:
    - path: "ai-specs/.commands/triage-playwright.md"
      provides: "Step 5: Persistir decisiones a archivo + commit+push"
      contains: "### 5. Persistir decisiones"
    - path: "ai-specs/.commands/triage-playwright.md"
      provides: "Sección Reglas actualizada"
      contains: "reason_match"
  key_links:
    - from: "Step 4 (triage interactivo)"
      to: "Step 5 (persistir)"
      via: "colectar decisiones en memoria y escribir al final"
      pattern: "Step 5.*Persistir"
    - from: "Step 5"
      to: "git commit+push"
      via: "bash invocado al finalizar el write"
      pattern: "git push"
---

<objective>
Extender el comando `/triage-playwright` (archivo `ai-specs/.commands/triage-playwright.md`) con un **Step 5: Persistir decisiones** que: (a) escribe un archivo `QA/{CLIENT}/{DATE}/triage-{date}.md` por cliente afectado, con el formato YAML frontmatter + secciones `##` + bloque fenced ```yaml (D-01..D-05), y (b) hace commit+push inmediato al main con mensaje descriptivo (D-12).

**Purpose:** Cerrar PROC-01 — capturar las decisiones de triage como artefactos committeados en git, para que sean auditables vía historia del repo y consumibles por `publish-results.py` (Plan 04-01).

**Output:** Archivo `ai-specs/.commands/triage-playwright.md` modificado con un nuevo Step 5 + bloque de Reglas actualizado. Al invocarse el comando, Claude genera el markdown exacto, lo guarda en la ruta correcta, lo commitea y lo pushea.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-persistence/04-CONTEXT.md
@.planning/phases/04-triage-persistence/04-RESEARCH.md

<!-- Archivo a modificar -->
@ai-specs/.commands/triage-playwright.md

<!-- Patrón de referencia (archivo QA/{CLIENT}/{DATE}/ ya existente) -->
@CLAUDE.md

<interfaces>
<!-- Convenciones del repo relevantes para el Step 5 -->

Directorio destino (D-07):
  QA/{CLIENT}/{YYYY-MM-DD}/triage-{YYYY-MM-DD}.md
  Donde {CLIENT} = display name del cliente (Codelpa, Bastien, Sonrie, Surtiventas, Prinorte, Soprole, Tienda) O el slug lowercase si así existe el directorio (`new-soprole`).

Global commit policy (CLAUDE.md):
  - push a main sin PRs
  - commit + push en un solo flujo
  - si remote rechaza → pull --rebase y reintentar

Granularidad (D-06 + D-08):
  - Una sección = un failure_group, NO un test individual
  - Si failure_group.clients = [c1, c2] → misma entrada aparece en ambos triage-{date}.md

Formato YAML (D-04):
  - reason_match: usar comillas DOBLES (permite \\ y \" escaping)
  - category: bug | flaky | ambiente (canonical de REQUIREMENTS.md PROC-01)
  - rationale: string libre, multi-línea OK (usar YAML `|` block scalar)
  - linear_ticket: YOM-NNN o null
  - action_taken: string libre
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Agregar Step 5 "Persistir decisiones" al final del flujo + actualizar Reglas con formato reason_match</name>
  <files>ai-specs/.commands/triage-playwright.md</files>

  <read_first>
    - ai-specs/.commands/triage-playwright.md (archivo completo — 139 líneas, entender estructura Step 1-5 actual)
    - .planning/phases/04-triage-persistence/04-CONTEXT.md (D-01..D-17 locked; especialmente D-01..D-08 para formato de archivo, D-12 para git flow)
    - .planning/phases/04-triage-persistence/04-RESEARCH.md (sección "Code Examples" Example 1 — template exacto del archivo; Example 4 — commit+push flow; "Common Pitfalls" #1 — escaping de reason_match)
    - CLAUDE.md (política global push-a-main)
  </read_first>

  <behavior>
    - Test 1 (structural): El archivo `ai-specs/.commands/triage-playwright.md` tiene una sección `### 5. Persistir decisiones — escribir triage file + commit+push` (o equivalente con número consistente) DESPUÉS del actual Step 4 y ANTES del actual `### 5. Resumen final` (que se renumera a Step 6 o se mueve).
    - Test 2 (template): La sección contiene un template literal con YAML frontmatter delimitado por `---`, título `# Triage — {CLIENT} — {DATE}`, y al menos un ejemplo de sección `##` con bloque ```yaml conteniendo las 5 claves (reason_match, category, rationale, linear_ticket, action_taken).
    - Test 3 (git flow): La sección contiene comandos `git add QA/...triage-...md`, `git commit -m "chore(triage): ..."`, `git push` (con fallback a `pull --rebase` + retry).
    - Test 4 (rules updated): La sección Reglas al final del archivo menciona explícitamente:
      - "reason_match debe copiarse EXACTAMENTE desde failure_group.reason"
      - "usar YAML double-quoted strings para reason_match (soporta \\ y \")"
      - "una sección por failure_group, no por test (D-06)"
      - "un archivo por cliente (D-07); si el group afecta varios clientes, replicar la misma sección en el archivo de cada cliente (D-08)"
  </behavior>

  <action>
**1. Renumerar la sección final actual.** El paso actual `### 5. Resumen final` (línea 108) se renombra a `### 6. Resumen final`. El nuevo paso a insertar va ENTRE el actual Step 4 (termina ~línea 106) y el renumerado Step 6.

**2. Insertar el siguiente bloque como nuevo Step 5.** Ubicación: después de la línea "**Skip**: pasar al siguiente fallo" (última línea del Step 4c) y antes de "### 5. Resumen final" (que pasa a ser Step 6).

```markdown
### 5. Persistir decisiones — escribir triage file + commit+push

Al completar la iteración del Step 4 (o después de cada sí/no/skip final), agrupar las decisiones por cliente y escribir un archivo markdown por cliente. Este paso es OBLIGATORIO — sin él, el triage se pierde al cerrar la sesión.

#### 5a. Agrupar decisiones por cliente (D-06, D-08)

Para cada `failure_group` donde el usuario decidió (no skip):
- Si `group.clients = [c1, c2, c3]` → esa decisión se replica en el archivo de c1, c2 y c3.
- Si `group.clients = [c1]` → solo en el archivo de c1.
- Granularidad: una decisión por `failure_group`, no por test individual.

Construir un dict `decisions_by_client = { slug: [ {reason_match, category, rationale, linear_ticket, action_taken}, ... ] }`.

#### 5b. Resolver ruta del archivo (maneja capitalización mixta)

Para cada slug en `decisions_by_client`:
- `{Display}` = leer `data/qa-matrix-staging.json` → `clients[{slug}-staging].name` → strip ` (staging)` → ej "Codelpa"
- Probar orden:
  1. Si existe `QA/{Display}/{DATE}/` → usar ese directorio
  2. Si existe `QA/{slug}/{DATE}/` (lowercase) → usar ese
  3. Si no existe ninguno → crear `QA/{Display}/{DATE}/` (preferir capitalizado por default)

Esto cubre el caso `new-soprole` (directorio en lowercase) sin romper los demás.

El nombre del archivo es siempre `triage-{DATE}.md` donde `{DATE}` = la fecha del run (el argumento pasado al comando, o la extraída de `public/history/{latest}.json`).

#### 5c. Generar contenido del archivo (template exacto)

Para cada cliente, el archivo sigue este template **exacto** (D-01..D-05):

```markdown
---
client: {slug}
date: {YYYY-MM-DD}
total_failures: {total_failures_del_run}
triaged_count: {count_de_decisions_para_ese_cliente}
triaged_by: Claude
---

# Triage — {Display} — {YYYY-MM-DD}

## {título_sección_1: reason truncado a ~80 chars}

` ``yaml
reason_match: "{reason completo del failure_group — copiado EXACTAMENTE}"
category: {bug|flaky|ambiente}
rationale: |
  {rationale multi-línea explicando la decisión}
linear_ticket: {YOM-NNN | null}
action_taken: "{acción tomada}"
` ``

## {título_sección_2}

` ``yaml
reason_match: "..."
category: ...
...
` ``
```

**Reglas críticas del template:**

- **reason_match DEBE ser idéntico a `failure_group.reason`**. No truncar, no editar. Usar YAML double-quoted string para permitir escape de backticks (`` ` ``), backslashes (`\\\\`), y símbolos regex (`$`, `*`, `.`). NUNCA usar single-quoted (no soporta `\\`).
- **El título de la sección `##`** es el `reason` truncado a ~80 chars (solo para legibilidad humana). La identidad real es `reason_match` dentro del bloque yaml.
- **Si el failure_group afecta varios clientes**, la misma sección (con el mismo reason_match) se escribe en cada archivo del cliente. `rationale` y `action_taken` son iguales por default; Eduardo puede editarlos manualmente post-commit si quiere granularidad por cliente.
- **Si `linear_ticket` es null**, escribir literalmente `linear_ticket: null` (sin comillas). El parser acepta null, ~, o ausencia.
- **Usar `rationale: |` (block scalar YAML)** para multi-línea. Indentar 2 espacios cada línea del contenido.
- **Si se re-ejecuta `/triage-playwright` el mismo día para el mismo cliente**: sobreescribir el archivo completo. Eduardo puede editarlo manualmente después; el próximo re-run lo pisa (aceptable — triage session = nueva decisión, mental model explícito).

#### 5d. Escribir archivos

```bash
mkdir -p "QA/{Display_o_slug}/{DATE}"
# Usar Write tool (no heredoc) para crear "QA/{Display_o_slug}/{DATE}/triage-{DATE}.md" con el contenido generado
```

Repetir por cada cliente en `decisions_by_client`.

#### 5e. Commit + push inmediato (D-12, CLAUDE.md global)

Después de escribir TODOS los archivos (un solo commit para todos los clientes del run):

```bash
cd /Users/lalojimenez/qa  # project root
git add QA/*/{DATE}/triage-{DATE}.md

# Derivar lista de clientes para el commit message
CLIENTS_STR=$(git diff --cached --name-only | sed -nE 's|QA/([^/]+)/.+|\1|p' | sort -u | paste -sd '+' -)
COUNT=$(git diff --cached --name-only | grep -c "triage-${DATE}.md")

git commit -m "chore(triage): ${CLIENTS_STR} ${DATE} — ${COUNT} failure_groups classified"
git push origin main || (git pull --rebase origin main && git push origin main)
```

**Ejemplos de commit message final:**
- Un solo cliente: `chore(triage): Codelpa 2026-04-17 — 3 failure_groups classified`
- Múltiples clientes: `chore(triage): Codelpa+Bastien+Sonrie 2026-04-17 — 7 failure_groups classified`

Si no hay decisiones que persistir (usuario dijo skip a todo) → omitir el Step 5 completo y saltar al Step 6 con nota "0 decisiones persistidas".
```

**3. Actualizar sección "Reglas"** (líneas 125-131). Agregar al final de la lista existente (después de "Si hay más de 5 fallos → priorizar…"):

```markdown
- **Persistir = obligatorio** — el Step 5 no es opcional. Sin archivo committeado, el triage no es auditable y no se integra al history JSON.
- **reason_match debe copiarse EXACTAMENTE** desde `failure_group.reason` — sin truncar, sin editar. Usar YAML double-quoted string para permitir escape correcto.
- **Una sección por `failure_group`, no por test individual** — el failure_group ya agrupa tests con misma causa raíz (D-06).
- **Un archivo por cliente afectado** — si un failure_group afecta a 3 clientes, la misma sección (mismo reason_match) va en los 3 archivos (D-08).
- **Re-run del mismo día sobreescribe el archivo** — edits manuales del usuario se pierden en el próximo triage session. Eduardo puede editar después del triage; los edits sobreviven hasta el próximo `/triage-playwright` del mismo cliente+fecha.
- **Commit+push es inmediato** — al final del Step 5, sin preguntar. Consistente con CLAUDE.md global.
```

**4. Actualizar sección "Archivos clave"** (línea 133). Agregar:

```markdown
- `QA/{CLIENT}/{DATE}/triage-{date}.md` — output del comando, committeado a git
- `tools/publish-results.py` — consumer del triage file (auto-lee en cada publish si el archivo existe)
```

**5. Ajustar "### 5. Resumen final" → "### 6. Resumen final"** (renumerar el encabezado). Actualizar el ejemplo del resumen para incluir la persistencia:

```markdown
### 6. Resumen final

```
─────────────────────────────────────────
Triage completado — {FECHA}

Acciones tomadas:
✓ Ticket Linear creado: {título} → {url}
✓ Spec parcheado: {archivo} — {descripción del cambio}
⏭ Saltado: {razón}
⚠️ Flaky monitoreados: {count}

Persistencia:
✓ {N} archivo(s) triage-{FECHA}.md escrito(s) en QA/
✓ Commit: chore(triage): {CLIENTES} {FECHA} — {N} failure_groups classified
✓ Push: origin main

Próximo paso: correr los tests parcheados para verificar
npx playwright test --grep "{tests parcheados}"
─────────────────────────────────────────
```
```
  </action>

  <verify>
    <automated>cd /Users/lalojimenez/qa && python3 -c "
from pathlib import Path
import re

path = Path('ai-specs/.commands/triage-playwright.md')
text = path.read_text(encoding='utf-8')

# 1. Step 5 exists with 'Persistir' in the heading
assert re.search(r'###\s*5\.\s*Persistir', text), 'Step 5 \"Persistir\" heading missing'

# 2. Step 5 appears BEFORE Step 6 (resumen renumbered)
m5 = re.search(r'^###\s*5\.\s*Persistir', text, re.MULTILINE)
m6 = re.search(r'^###\s*6\.\s*Resumen', text, re.MULTILINE)
assert m5 and m6, f'Both steps required. m5={bool(m5)} m6={bool(m6)}'
assert m5.start() < m6.start(), 'Step 5 must precede Step 6'

# 3. Template frontmatter keys present
for key in ['client:', 'date:', 'total_failures:', 'triaged_count:', 'triaged_by:']:
    assert key in text, f'Frontmatter key missing: {key}'

# 4. Section yaml block fields present
for field in ['reason_match:', 'category:', 'rationale:', 'linear_ticket:', 'action_taken:']:
    assert field in text, f'Section field missing: {field}'

# 5. Git flow commands
assert 'git add QA/' in text, 'git add missing'
assert 'git commit -m' in text, 'git commit missing'
assert 'chore(triage):' in text, 'commit prefix missing'
assert 'git push' in text, 'git push missing'
assert 'pull --rebase' in text, 'rebase fallback missing'

# 6. Reglas updated
for rule in ['reason_match debe copiarse EXACTAMENTE', 'double-quoted', 'D-06', 'D-08', 'Commit+push es inmediato']:
    assert rule in text, f'Rule snippet missing: {rule}'

# 7. QA path pattern in Archivos clave
assert 'QA/{CLIENT}/{DATE}/triage-{date}.md' in text, 'triage file path missing from Archivos clave'
assert 'tools/publish-results.py' in text, 'consumer reference missing'

# 8. No broken heading numbering (no two '### 5.' top-level headings)
step5_count = len(re.findall(r'^###\s*5\.\s', text, re.MULTILINE))
step6_count = len(re.findall(r'^###\s*6\.\s', text, re.MULTILINE))
assert step5_count == 1, f'Expected exactly one \"### 5.\" heading, got {step5_count}'
assert step6_count == 1, f'Expected exactly one \"### 6.\" heading, got {step6_count}'

print('OK: all 8 structural checks passed')
"</automated>
  </verify>

  <done>
    - `ai-specs/.commands/triage-playwright.md` tiene un nuevo `### 5. Persistir decisiones` insertado entre el actual Step 4 y el anteriormente llamado "### 5. Resumen final" (ahora `### 6. Resumen final`)
    - El template de archivo incluye YAML frontmatter (client, date, total_failures, triaged_count, triaged_by) + al menos una sección `##` con bloque ```yaml conteniendo las 5 claves canónicas
    - Los comandos `git add QA/.../triage-{DATE}.md`, `git commit -m "chore(triage): {CLIENTES} {DATE} — {N} failure_groups classified"`, `git push origin main || (git pull --rebase origin main && git push origin main)` están presentes
    - La sección Reglas incluye explícitamente: reason_match exacto, double-quoted, D-06 (una sección por failure_group), D-08 (replicar por cliente), commit+push inmediato
    - La sección "Archivos clave" lista `QA/{CLIENT}/{DATE}/triage-{date}.md` y referencia `tools/publish-results.py` como consumer
    - El script de verify pasa sus 8 checks estructurales
    - Los Steps 1-4 existentes NO fueron modificados en contenido (solo el encabezado `### 5.` se movió a `### 6.`)
  </done>
</task>

</tasks>

<verification>
**Structural gate (ejecutado en el verify del task):**
- Step 5 existe con título "Persistir"
- Step 5 precede a Step 6 (renumerado)
- Frontmatter + section yaml fields presentes en el template
- Comandos git presentes (add, commit, push, rebase fallback)
- Reglas actualizadas con snippets específicos
- Heading numbering sin duplicados

**Semantic gate (revisión manual post-plan):**
- Re-leer el archivo completo y confirmar que el flujo de lectura humana es coherente: Step 1 (adoptar rol) → 2 (cargar) → 3 (resumen) → 4 (triagear c/u) → 5 (persistir) → 6 (resumen final con persistencia reflejada)
- Verificar que el ejemplo del template usa `|` block scalar para rationale multi-línea

**Interop gate con Plan 04-01:**
- El `reason_match` generado según este plan matcheará `failure_group.reason` tal como lo lee `_apply_triage_overlay`. Ambos planes usan comparación `==` exacta.
</verification>

<success_criteria>
**Cubre success criteria #1 de Phase 4:**
- #1: Running `/triage-playwright` produces `QA/{CLIENT}/{DATE}/triage-{date}.md` with one entry per failure classifying it as `bug`, `flaky`, or `ambiente` + rationale ✓

**Requerimientos cerrados:** PROC-01.

**Decisiones honradas:** D-01 (YAML fm+sections), D-02 (frontmatter), D-03 (sección título truncado + reason_match completo), D-04 (4 campos: category, rationale, linear_ticket, action_taken), D-05 (reason_match para matching exacto), D-06 (granularidad por failure_group), D-07 (un archivo por cliente), D-08 (replicar sección en múltiples clientes), D-12 (commit+push inmediato con message format), D-14 (editable manualmente — post-commit permanece hasta próximo re-run).

**Interop con Plan 04-01:** Formato escrito aquí es el mismo formato leído por `_parse_triage_file`. Matching exacto `reason_match` == `failure_group.reason` funciona porque el Step 5 manda copiar el reason sin editar.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude session → filesystem (`Write` tool) | Claude genera contenido de markdown y lo escribe en el repo local. Contenido controlado por lógica del comando. |
| Local git → remote (GitHub) | Push a main. El archivo triage-{date}.md queda en historia pública del repo (repo privado YOM). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Information Disclosure | `rationale`/`action_taken` committeados a git | mitigate | Documentar en la sección Reglas: "Nunca pegar credentials, tokens, ni respuestas de API raw en rationale. Para snippets de error usar placeholders `[REDACTED]`." Complementa el filtro runtime de Plan 04-01 (`mask_secrets` en publish-results.py). |
| T-04-07 | Tampering (git push race con otra sesión) | git push sin sync previo | mitigate | Fallback `pull --rebase && push` cubre el caso estándar. Si el rebase falla por conflicto (ambos lados editaron el mismo triage-{date}.md), Claude debe abortar y reportar al usuario — no forzar. |
| T-04-08 | Repudiation | Triage sin autor identificable | mitigate | Frontmatter field `triaged_by: Claude` (o usuario identificado) + git commit author = Eduardo. Doble trazabilidad. |
| T-04-09 | Denial of Service | Archivo triage enorme (miles de secciones) | accept | El número de failure_groups por run está acotado por el tamaño del suite Playwright (<50 en práctica). No es un vector real. |
</threat_model>

<output>
Al terminar crear:
`.planning/phases/04-triage-persistence/04-02-SUMMARY.md`
</output>
