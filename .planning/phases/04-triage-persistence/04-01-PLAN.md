---
phase: 04-triage-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/publish-results.py
autonomous: true
requirements:
  - PROC-02
tags:
  - python
  - yaml
  - pipeline
  - triage
  - publish-results
must_haves:
  truths:
    - "publish-results.py carga QA/{Display}/{DATE}/triage-{date}.md (o fallback QA/{slug}/{DATE}/) cuando existe"
    - "Cada failure_group que matchea un reason_match del triage file gana un campo triage con 6 subcampos"
    - "Si no hay triage file, publish-results.py funciona exactamente como antes (sin campo triage, sin errores)"
    - "YAML inválido loggea warning por stderr y continúa sin triage"
    - "Sección huérfana (reason_match no matchea) loggea warning con slug+reason y continúa"
    - "rationale y action_taken pasan por mask_secrets() antes de embeberse en history JSON"
    - "El overlay se aplica dentro de generate_run_json() antes del return — sobrevive merge_run_json"
  artifacts:
    - path: "tools/publish-results.py"
      provides: "3 helpers nuevos + invocación en generate_run_json"
      contains: "_triage_file_path"
    - path: "tools/publish-results.py"
      provides: "Parser YAML de archivo triage"
      contains: "_parse_triage_file"
    - path: "tools/publish-results.py"
      provides: "Overlay mutator sobre failure_groups"
      contains: "_apply_triage_overlay"
    - path: "tools/publish-results.py"
      provides: "Import de PyYAML"
      contains: "import yaml"
  key_links:
    - from: "generate_run_json()"
      to: "_apply_triage_overlay()"
      via: "invocación después de generate_failure_groups() y antes del return"
      pattern: "_apply_triage_overlay\\(failure_groups"
    - from: "_apply_triage_overlay()"
      to: "_parse_triage_file()"
      via: "parsing YAML frontmatter + secciones"
      pattern: "_parse_triage_file\\("
    - from: "_apply_triage_overlay()"
      to: "_triage_file_path()"
      via: "resolver QA/{Display|slug}/{date}/triage-{date}.md"
      pattern: "_triage_file_path\\("
    - from: "_apply_triage_overlay()"
      to: "mask_secrets()"
      via: "redactar credenciales de rationale y action_taken"
      pattern: "mask_secrets\\(section"
---

<objective>
Agregar al pipeline de publish (`tools/publish-results.py`) un overlay opcional que lee archivos `QA/{CLIENT}/{DATE}/triage-{date}.md` (si existen) y agrega un campo `triage` a cada `failure_group` del `history/{date}.json` para el cliente correspondiente.

**Purpose:** Hacer persistentes y auditables las decisiones de triage (`bug` / `flaky` / `ambiente`) que hoy se pierden en el chat. Cierra requerimiento PROC-02.

**Output:** `tools/publish-results.py` modificado con `import yaml` + 3 helpers privados + una llamada desde `generate_run_json()`. Efecto: cuando existe un triage file válido, `public/history/{date}.json` contiene `failure_groups[].triage` con categoría humana, rationale, linear_ticket, action_taken, triaged_at, triaged_by.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-triage-persistence/04-CONTEXT.md
@.planning/phases/04-triage-persistence/04-RESEARCH.md

<!-- Source a modificar -->
@tools/publish-results.py

<!-- Data structure real (usar para validar matching) -->
@public/history/2026-04-17.json

<interfaces>
<!-- Signatures existentes relevantes en publish-results.py -->

From tools/publish-results.py:
```python
# Line 143 — secret redaction (reusar para rationale/action_taken)
def mask_secrets(text: str) -> str: ...

# Line 487 — slug → {name, url} lookup
def load_staging_urls(project_root: Path) -> dict: ...
# Returns: { "bastien": {"name": "Bastien (staging)", "url": "https://..."}, ... }

# Line 546 — donde se inyecta el overlay
def generate_run_json(results: dict, date: str, project_root: Path = None) -> dict:
    # ... existing code ...
    # Line 628: "failure_groups": generate_failure_groups(results),
    # El overlay debe aplicarse sobre el objeto que se pone en esa clave,
    # ANTES de retornar el dict.

# Line 698 — merge logic (no modificar — el overlay pre-merge sobrevive)
def merge_run_json(existing: dict, new: dict) -> dict: ...
```

Estructura de un failure_group existente (verificado contra `public/history/2026-04-17.json`):
```json
{
  "category": "bug",
  "label": "🔴 Bug",
  "reason": "Redirección incorrecta en: Pipeline API catálogo ...",
  "owner": "dev",
  "action": "Revisar el routing ...",
  "count": 1,
  "tests": ["..."],
  "clients": ["sonrie"],
  "error_sample": "...",
  "annotations_sample": [],
  "spec_file": "mongo-data.spec.ts"
}
```

Campos nuevos a agregar (solo cuando hay match):
```json
"triage": {
  "category": "bug" | "flaky" | "ambiente",
  "rationale": "string libre (multi-línea OK)",
  "linear_ticket": "YOM-234" | null,
  "action_taken": "string libre",
  "triaged_at": "YYYY-MM-DD",
  "triaged_by": "Claude" | "<usuario>"
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Import yaml y agregar 3 helpers privados (_triage_file_path, _parse_triage_file, _apply_triage_overlay)</name>
  <files>tools/publish-results.py</files>

  <read_first>
    - tools/publish-results.py (líneas 1-20 para confirmar bloque de imports, líneas 485-545 para reusar load_staging_urls y patrón de helpers privados, líneas 123-150 para mask_secrets)
    - .planning/phases/04-triage-persistence/04-RESEARCH.md (sección "Pattern 1: YAML Frontmatter + Fenced Section Body" — código canónico del parser; "Pattern 2: Overlay" — estructura del overlay; "Pattern 3: slug → display name" — lookup de path; "Security Domain" — aplicar mask_secrets a rationale/action_taken)
    - .planning/phases/04-triage-persistence/04-CONTEXT.md (D-01..D-17 — decisiones locked)
    - public/history/2026-04-17.json (validar forma de failure_group.reason y .clients)
  </read_first>

  <behavior>
    - Test 1 (dry): `python3 -c "from tools.publish_results import _triage_file_path, _parse_triage_file, _apply_triage_overlay"` importa las 3 funciones sin error.
    - Test 2 (dry): Con un triage file válido con 2 secciones, `_parse_triage_file(path)` retorna `(frontmatter_dict, [section_dict, section_dict])`. Cada section_dict tiene las claves `reason_match`, `category`, `rationale`, `linear_ticket`, `action_taken`.
    - Test 3 (fallback): Con un archivo que solo tiene frontmatter sin secciones, `_parse_triage_file(path)` retorna `({...}, [])` sin lanzar excepción.
    - Test 4 (fail-safe YAML): Con un archivo con YAML inválido, `_parse_triage_file(path)` retorna `({}, [])` e imprime warning por stderr. No lanza excepción (D-16).
    - Test 5 (missing file): Con un path inexistente, `_apply_triage_overlay(groups, date, root)` es no-op — no muta groups, no imprime error (D-15).
    - Test 6 (orphan section): Con un triage file cuyo reason_match no matchea ningún failure_group, loggea warning con el slug y los primeros ~60 chars del reason_match, y no muta ese group (D-17).
    - Test 7 (match happy path): Con un triage file cuyo `reason_match` === `failure_group.reason` para el slug correcto, el group adquiere `triage` con los 6 subcampos, y `rationale` + `action_taken` pasaron por `mask_secrets()`.
    - Test 8 (display vs slug fallback): Si `QA/{Display}/{date}/triage-{date}.md` no existe pero `QA/{slug}/{date}/triage-{date}.md` sí, el helper lo usa (cubre el caso `new-soprole`).
  </behavior>

  <action>
**1. Agregar import.** Insertar en el bloque de imports (después de `import json` en línea 13, antes de `import re`):

```python
import yaml
```

**2. Insertar el helper `_triage_file_path`** justo después de `load_staging_urls()` (después de la línea 504). Debe devolver el primer path existente entre `QA/{Display}/{date}/` y `QA/{slug}/{date}/`. Si ninguno existe, retorna el de `{Display}` (para que `.exists()` dé False y el overlay haga skip):

```python
def _triage_file_path(slug: str, date: str, project_root: Path,
                      staging_urls: dict) -> Path:
    """Resolve QA/{Display|slug}/{date}/triage-{date}.md.

    Tries the display name derived from qa-matrix-staging.json first, then
    falls back to the raw slug if that directory doesn't exist. This handles
    new-soprole and any future client with lowercase dir convention.
    """
    info = staging_urls.get(slug, {})
    raw_name = info.get("name", slug.capitalize())
    display = re.sub(r"\s*\(staging\)\s*$", "", raw_name).strip() or slug.capitalize()

    by_display = project_root / "QA" / display / date / f"triage-{date}.md"
    by_slug = project_root / "QA" / slug / date / f"triage-{date}.md"

    if by_display.exists():
        return by_display
    if by_slug.exists():
        return by_slug
    # Neither exists — return display path so caller's .exists() returns False
    return by_display
```

**3. Insertar el helper `_parse_triage_file`** inmediatamente después de `_triage_file_path`. Devuelve `(frontmatter_dict, list_of_section_dicts)`. Cada section_dict incluye `_title` (el título del `## …`). En cualquier error de IO o YAML → warning + retornar `({}, [])`:

```python
def _parse_triage_file(path: Path) -> tuple:
    """Parse a triage-{date}.md file into (frontmatter, sections).

    Format:
      ---
      <YAML frontmatter>
      ---
      ## <title>
      ```yaml
      reason_match: "..."
      category: bug|flaky|ambiente
      rationale: "..."
      linear_ticket: YOM-NNN|null
      action_taken: "..."
      ```

    Returns ({}, []) on any IO / parse error after logging a warning to stderr.
    Missing sections with invalid YAML are skipped individually (not fatal).
    """
    try:
        text = path.read_text(encoding="utf-8")
    except OSError as e:
        print(f"⚠️  No se pudo leer triage file {path}: {e}", file=sys.stderr)
        return ({}, [])

    fm_match = re.match(r"^---\n(.*?)\n---\n(.*)$", text, re.DOTALL)
    if not fm_match:
        print(f"⚠️  {path}: falta YAML frontmatter — skip triage file",
              file=sys.stderr)
        return ({}, [])

    try:
        frontmatter = yaml.safe_load(fm_match.group(1)) or {}
    except yaml.YAMLError as e:
        print(f"⚠️  {path}: YAML frontmatter inválido ({e}) — skip triage file",
              file=sys.stderr)
        return ({}, [])
    if not isinstance(frontmatter, dict):
        frontmatter = {}

    body = fm_match.group(2)

    # Matches: "## <title>\n ... ```yaml\n<body>\n``` "
    section_pattern = re.compile(
        r"^##\s+(?P<title>.+?)\n\s*```yaml\n(?P<body>.*?)\n```",
        re.MULTILINE | re.DOTALL,
    )
    sections = []
    for m in section_pattern.finditer(body):
        raw_body = m.group("body")
        try:
            data = yaml.safe_load(raw_body) or {}
        except yaml.YAMLError as e:
            print(f"⚠️  {path}: sección '{m.group('title')[:60]}' "
                  f"con YAML inválido ({e}) — skip section", file=sys.stderr)
            continue
        if not isinstance(data, dict):
            continue
        data["_title"] = m.group("title").strip()
        sections.append(data)

    return (frontmatter, sections)
```

**4. Insertar el helper `_apply_triage_overlay`** inmediatamente después de `_parse_triage_file`. Itera sobre slugs únicos de `failure_groups`, carga el triage file por slug, y muta in place los groups con match exacto (`==`) contra `reason_match`. Orphans → warning. Aplica `mask_secrets()` a `rationale` y `action_taken`:

```python
def _apply_triage_overlay(failure_groups: list, date: str,
                          project_root: Path) -> None:
    """Mutate failure_groups in place, adding a 'triage' field where a
    matching QA/{client}/{date}/triage-{date}.md section exists.

    D-15: Missing files are silently ignored (no regression).
    D-16: Invalid YAML logs a warning and continues without triage.
    D-17: Orphan sections (no matching failure_group) log a warning and are
          ignored.

    The overlay MUST run before merge_run_json: by applying it inside
    generate_run_json() the new run's failure_groups already carry the triage
    field when the merge logic (line 728-733 of this file) replaces
    per-client groups.
    """
    if not failure_groups:
        return

    staging_urls = load_staging_urls(project_root)

    # Unique client slugs that appear in this run's failure_groups
    slugs = {c for g in failure_groups for c in g.get("clients", [])}

    # Index by (slug, reason) → group for O(1) matching
    index = {}
    for g in failure_groups:
        reason = g.get("reason", "")
        for slug in g.get("clients", []):
            index[(slug, reason)] = g

    n_files = 0
    n_sections_matched = 0
    n_orphans = 0

    for slug in sorted(slugs):
        path = _triage_file_path(slug, date, project_root, staging_urls)
        if not path.exists():
            continue
        n_files += 1

        frontmatter, sections = _parse_triage_file(path)
        if not sections:
            continue

        triaged_at = str(frontmatter.get("date") or date)
        triaged_by = str(frontmatter.get("triaged_by", "Claude"))

        for section in sections:
            reason_match = section.get("reason_match")
            if not isinstance(reason_match, str) or not reason_match.strip():
                print(f"⚠️  {path}: sección '{section.get('_title','?')[:60]}' "
                      f"sin reason_match — skip", file=sys.stderr)
                continue

            group = index.get((slug, reason_match))
            if group is None:
                n_orphans += 1
                print(f"⚠️  {path}: reason_match "
                      f"'{reason_match[:60]}...' no coincide con ningún "
                      f"failure_group para {slug} — skip", file=sys.stderr)
                continue

            rationale = section.get("rationale")
            action_taken = section.get("action_taken")
            if isinstance(rationale, str):
                rationale = mask_secrets(rationale)
            if isinstance(action_taken, str):
                action_taken = mask_secrets(action_taken)

            group["triage"] = {
                "category": section.get("category"),
                "rationale": rationale,
                "linear_ticket": section.get("linear_ticket"),
                "action_taken": action_taken,
                "triaged_at": triaged_at,
                "triaged_by": triaged_by,
            }
            n_sections_matched += 1

    if n_files:
        print(f"ℹ️  Triage aplicado: {n_files} archivo(s), "
              f"{n_sections_matched} sección(es) matched, "
              f"{n_orphans} huérfana(s)")
```

**Ubicación exacta:** Los 3 helpers van consecutivos entre `load_staging_urls()` (termina ~línea 504) y `extract_config_validation_clients()` (empieza ~línea 507). El orden es _triage_file_path → _parse_triage_file → _apply_triage_overlay (dependencias locales: el overlay usa los otros dos; el parser es independiente).

**NO tocar:**
- `classify_error` (líneas 161-359)
- `generate_failure_groups` (líneas 361-438)
- `merge_run_json` (líneas 698-749)
- `main()` (línea 752+)
  </action>

  <verify>
    <automated>cd /Users/lalojimenez/qa && python3 -c "
import sys, json, tempfile, importlib.util
from pathlib import Path

spec = importlib.util.spec_from_file_location('pr', 'tools/publish-results.py')
pr = importlib.util.module_from_spec(spec)
spec.loader.exec_module(pr)

# 1. imports succeed
assert hasattr(pr, 'yaml'), 'yaml not imported'
assert hasattr(pr, '_triage_file_path'), 'missing _triage_file_path'
assert hasattr(pr, '_parse_triage_file'), 'missing _parse_triage_file'
assert hasattr(pr, '_apply_triage_overlay'), 'missing _apply_triage_overlay'

# 2. parse happy path
with tempfile.TemporaryDirectory() as d:
    p = Path(d) / 'triage.md'
    p.write_text('''---
client: codelpa
date: 2026-04-17
total_failures: 1
triaged_count: 1
triaged_by: Claude
---

## Sample failure

\`\`\`yaml
reason_match: \"API retorna 500 en /pipeline/catalog\"
category: bug
rationale: \"Endpoint rompe con payload vacio\"
linear_ticket: YOM-234
action_taken: \"Ticket creado\"
\`\`\`
''')
    fm, secs = pr._parse_triage_file(p)
    assert fm.get('client') == 'codelpa', f'fm broken: {fm}'
    assert len(secs) == 1, f'expected 1 section, got {len(secs)}'
    assert secs[0]['reason_match'] == 'API retorna 500 en /pipeline/catalog'
    assert secs[0]['category'] == 'bug'
    assert secs[0]['linear_ticket'] == 'YOM-234'

# 3. invalid YAML -> ({}, [])
with tempfile.TemporaryDirectory() as d:
    p = Path(d) / 'bad.md'
    p.write_text('---\ncliente: : :\n  broken\n---\n')
    fm, secs = pr._parse_triage_file(p)
    assert fm == {} and secs == [], 'invalid YAML must return empty tuple'

# 4. overlay happy path with mask_secrets integration
groups = [{'category': 'bug', 'reason': 'X matches', 'clients': ['bastien']}]
with tempfile.TemporaryDirectory() as d:
    root = Path(d)
    qa = root / 'QA' / 'bastien' / '2026-04-17'
    qa.mkdir(parents=True)
    (qa / 'triage-2026-04-17.md').write_text('''---
client: bastien
date: 2026-04-17
triaged_by: Claude
---

## X

\`\`\`yaml
reason_match: \"X matches\"
category: flaky
rationale: \"retry 3/4 token=abc123defghi\"
linear_ticket: null
action_taken: \"Monitorear\"
\`\`\`
''')
    # stub staging_urls via empty dict so _triage_file_path falls back to slug
    pr.load_staging_urls = lambda r: {}
    pr._apply_triage_overlay(groups, '2026-04-17', root)
    t = groups[0].get('triage')
    assert t is not None, f'triage not applied: {groups[0]}'
    assert t['category'] == 'flaky'
    assert t['linear_ticket'] is None
    assert t['triaged_by'] == 'Claude'

# 5. missing file is a no-op
groups2 = [{'category': 'bug', 'reason': 'Y', 'clients': ['nonexistent']}]
with tempfile.TemporaryDirectory() as d:
    pr._apply_triage_overlay(groups2, '2026-04-17', Path(d))
    assert 'triage' not in groups2[0], 'no-op expected'

print('OK: all 5 checks passed')
"</automated>
  </verify>

  <done>
    - `import yaml` presente en el bloque de imports superior de `tools/publish-results.py`
    - Los 3 helpers (`_triage_file_path`, `_parse_triage_file`, `_apply_triage_overlay`) existen como funciones de módulo
    - Todos los checks del script de verify pasan (happy path parse, invalid-YAML returns empty, overlay muta group con 6 subcampos, missing file es no-op)
    - `mask_secrets()` se aplica a `rationale` y `action_taken` (verificable por grep: `mask_secrets(rationale)` y `mask_secrets(action_taken)` aparecen dentro de `_apply_triage_overlay`)
    - Ninguna función pre-existente fue modificada (classify_error, generate_failure_groups, merge_run_json, main permanecen bit-a-bit idénticas a pre-plan)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Invocar _apply_triage_overlay dentro de generate_run_json (antes del return) + validar regresión cero con data real</name>
  <files>tools/publish-results.py</files>

  <read_first>
    - tools/publish-results.py (líneas 546-635 — `generate_run_json`, específicamente el punto donde se construye `failure_groups` en línea 628; confirmar que el dict se retorna al final)
    - .planning/phases/04-triage-persistence/04-RESEARCH.md (sección "Example 3: Invocación desde generate_run_json()" — patrón exacto; "Common Pitfalls" #3 — por qué overlay debe ir pre-merge, no post)
    - public/history/2026-04-17.json (el `failure_groups` actual — verificar post-run que los shapes existentes no cambiaron)
  </read_first>

  <behavior>
    - Test 1 (integration no-regression): Corriendo `publish-results.py` sin ningún triage file en `QA/*/`, el output `public/history/{date}.json` tiene exactamente la misma estructura que antes — sin campo `triage` en ningún `failure_group`, mismos campos preexistentes intactos.
    - Test 2 (integration with triage): Con un triage file válido en `QA/Codelpa/2026-04-17/triage-2026-04-17.md` matcheando un reason real, `public/history/2026-04-17.json.failure_groups[i].triage` existe con los 6 subcampos.
    - Test 3 (merge preservation): Después del overlay, `merge_run_json` no borra el campo `triage` porque el overlay se aplicó al new run_json ANTES del merge.
    - Test 4 (overlay runs exactly once per generate_run_json): No hay doble overlay — el campo `triage` no aparece duplicado/triplicado.
  </behavior>

  <action>
**1. Refactor defensivo primero.** En `generate_run_json()`, la línea 628 hoy dice:

```python
"failure_groups": generate_failure_groups(results),
```

Esto construye el list inline. Necesitamos acceder al list antes de retornar para pasárselo al overlay. Refactorizar así — reemplazar el bloque final del `return` (líneas 619-634) por:

```python
    # Generate failure groups and apply optional triage overlay (Phase 4 / PROC-02).
    # Overlay MUST run before merge_run_json so triage survives per-client merge
    # (see Common Pitfall #3 in 04-RESEARCH.md).
    failure_groups = generate_failure_groups(results)
    _apply_triage_overlay(failure_groups, date, project_root)

    return {
        "date": date,
        "timestamp": datetime.now(timezone.utc).isoformat(),
        "total": total_tests,
        "passed": total_passed,
        "failed": total_failed,
        "duration": duration,
        "suites": suites,
        "clients": clients,
        "failure_groups": failure_groups,
        "pending_b2b": generate_pending_b2b(results),
        "evidence": {
            "screenshots": [],
            "errors": [],
        },
    }
```

**2. No tocar nada más.** `merge_run_json` ya preserva `triage` porque reemplaza el group completo cuando el cliente aparece en el new run (líneas 728-733). El overlay ya puso `triage` en el group reemplazante.

**3. Smoke con data real (no romper regresión).** Correr el script contra `tests/e2e/playwright-report/results.json` (o una fixture equivalente) y comparar que los campos preexistentes de `failure_groups[]` siguen idénticos cuando no hay triage file.
  </action>

  <verify>
    <automated>cd /Users/lalojimenez/qa && python3 -c "
import importlib.util, inspect, json
from pathlib import Path

spec = importlib.util.spec_from_file_location('pr', 'tools/publish-results.py')
pr = importlib.util.module_from_spec(spec)
spec.loader.exec_module(pr)

# 1. generate_run_json contains the overlay call
src = inspect.getsource(pr.generate_run_json)
assert '_apply_triage_overlay(' in src, 'overlay not invoked in generate_run_json'
assert 'failure_groups = generate_failure_groups(' in src, 'refactor missing — failure_groups must be named var'
# 2. overlay called BEFORE return (failure_groups assignment then overlay then return)
i_assign = src.index('failure_groups = generate_failure_groups(')
i_overlay = src.index('_apply_triage_overlay(')
i_return = src.index('return {')
assert i_assign < i_overlay < i_return, 'order must be: assign failure_groups -> overlay -> return'

# 3. No regression: run against existing history file — structure must include failure_groups list
hist = Path('public/history/2026-04-17.json')
if hist.exists():
    with open(hist) as f:
        data = json.load(f)
    fg = data.get('failure_groups', [])
    assert isinstance(fg, list), 'failure_groups not a list'
    # Required fields pre-existed — phase 4 only ADDS 'triage', never removes
    required = {'category', 'reason', 'clients'}
    for g in fg:
        missing = required - set(g.keys())
        assert not missing, f'required field lost: {missing} in {g}'

print('OK: overlay wired correctly, no regression')
" && python3 tools/publish-results.py --date 2026-04-17 2>&1 | tail -20</automated>
  </verify>

  <done>
    - `generate_run_json()` invoca `_apply_triage_overlay(failure_groups, date, project_root)` entre la asignación `failure_groups = generate_failure_groups(results)` y el `return {...}`
    - El bloque del return pasa `failure_groups` (la variable, no la llamada inline) al dict
    - Un run de `python3 tools/publish-results.py --date 2026-04-17` termina sin excepción y reescribe `public/history/2026-04-17.json` con los mismos campos preexistentes (el campo `triage` solo aparece cuando hay triage file válido — para 2026-04-17 sin triage file no debe aparecer, validando D-15)
    - Los campos `category`, `reason`, `clients`, `count`, `tests`, `error_sample`, `spec_file` de cada `failure_group` preexistente permanecen intactos (no-regresión)
  </done>
</task>

</tasks>

<verification>
**Regression gate:**
- `python3 tools/publish-results.py --date 2026-04-17` corre sin errores cuando no hay `QA/*/2026-04-17/triage-2026-04-17.md`
- `public/history/2026-04-17.json` pre-plan y post-plan (sin triage file) son estructuralmente equivalentes — ninguna clave removida, ningún valor de failure_group cambiado

**Positive gate (manual after Plan 04-02 lands):**
- Crear manualmente `QA/Codelpa/2026-04-17/triage-2026-04-17.md` con 1 sección matching un reason real → correr publish-results → verificar que `public/history/2026-04-17.json.failure_groups[].triage` aparece con los 6 subcampos

**Security gate:**
- Grep `mask_secrets(rationale)` y `mask_secrets(action_taken)` están dentro de `_apply_triage_overlay`
- Grep `yaml.load(` (sin `safe_`) no aparece en el archivo — solo `yaml.safe_load`
</verification>

<success_criteria>
**Cubre success criteria #2, #3, #4 de Phase 4:**
- #2: `tools/publish-results.py` detecta triage file y incorpora decisiones en `history/{date}.json` ✓
- #3: Sin triage file → no regresión ✓
- #4: Un usuario inspeccionando `history/{date}.json` ve el triage (campo `triage` por failure_group)

**Requerimientos cerrados:** PROC-02.

**Decisiones honradas:** D-01 (YAML fm+sections), D-02 (frontmatter opcional), D-03/04/05 (sección por failure_group, 4 campos + reason_match), D-09 (overlay no override), D-10 (overlay se agrega siempre), D-13 (re-lee cada publish), D-15 (missing = no-op), D-16 (YAML inválido = warning), D-17 (orphan = warning). D-06/07/08/11/12/14 aplican al Plan 04-02.

**Mitigación security (research §Security Domain):** `yaml.safe_load` (nunca `yaml.load`), `isinstance(data, dict)` tras parse, `mask_secrets` sobre rationale + action_taken.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| triage-{date}.md file → publish-results.py | Archivo del repo, editable manualmente por Eduardo (D-14). Se parsea como YAML. |
| publish-results.py → public/history/{date}.json | JSON publicado en GitHub Pages (repo público). Contenido visible para cualquiera con acceso al repo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | `_parse_triage_file` (YAML parse) | mitigate | Usar exclusivamente `yaml.safe_load()`; prohibido `yaml.load()`/`Loader=yaml.Loader`. Validar `isinstance(data, dict)` tras cada parse. |
| T-04-02 | Information Disclosure | `rationale`/`action_taken` publicados en history JSON | mitigate | Aplicar `mask_secrets()` (ya existente en el módulo) a ambos campos antes del overlay final. Cubre Bearer tokens, sk-, ghp_, AWS keys, JWT, password=, mongodb://. |
| T-04-03 | Tampering (Path traversal vía slug controlado por data externa) | `_triage_file_path` | accept | Slugs vienen de `failure_groups[].clients`, derivados de títulos de tests (`"bastien: ..."`) y de `qa-matrix-staging.json` controlado por el repo. No hay entrada externa de usuario. `Path(...) / segment` sin `os.path.join` con strings concatenadas limita la superficie. |
| T-04-04 | Denial of Service (YAML bomb via billion laughs) | `_parse_triage_file` | accept | `yaml.safe_load` no expande anchors recursivos a serialización explosiva por default. Archivo triage es controlado por el repo (editado por Claude/Eduardo). Riesgo operacional mínimo. |
| T-04-05 | Repudiation | Dashboard muestra `triage.category` sin distinguir fuente | accept | `triage.triaged_by` queda registrado en el JSON publicado. Auditable post-hoc por commit history del triage-{date}.md. |
</threat_model>

<output>
Al terminar crear:
`.planning/phases/04-triage-persistence/04-01-SUMMARY.md`
</output>
