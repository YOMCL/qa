---
phase: 06-actionable-reports-agent-precision
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-specs/.agents/maestro-specialist.md
autonomous: true
requirements:
  - AGENT-04
  - AGENT-05

must_haves:
  truths:
    - "El maestro-specialist sabe exactamente qué hacer cuando un flow falla sus 3 reintentos automáticos"
    - "Un manual-pass siempre queda documentado con razón, aprobador y fecha en el HTML del reporte"
    - "Hay cero ambigüedad sobre cuándo activar manual-pass vs seguir depurando"
  artifacts:
    - path: "ai-specs/.agents/maestro-specialist.md"
      provides: "Protocolo de 3 reintentos + instrucción de logging manual-pass"
      contains: "## 3-Retry Protocol"
  key_links:
    - from: "maestro-specialist.md ## 3-Retry Protocol"
      to: "decisión de manual-pass"
      via: "3 pasos: leer log → clasificar → activar si no corregible"
      pattern: "manual-pass"
    - from: "manual-pass logging"
      to: "HTML del reporte Maestro"
      via: "sección ## Manual-pass decisions en el HTML"
      pattern: "Manual-pass decisions"
---

<objective>
Agregar dos secciones a `ai-specs/.agents/maestro-specialist.md`: (1) `## 3-Retry Protocol` que define el flujo de decisión cuando un flow agota sus 3 reintentos automáticos antes de activar manual-pass, y (2) `## Manual-Pass Logging` que define cómo y dónde registrar la decisión de manual-pass en el HTML del reporte Maestro existente.

Purpose: Actualmente el specialist no tiene protocolo para los fallos persistentes — cada instancia de Claude improvisa la decisión. Con el protocolo de 3 pasos (leer log → clasificar → activar manual-pass solo si no corregible en el momento), se reduce el número de manual-passes innecesarios y se garantiza que los que sí ocurren queden trazados.

Output: `ai-specs/.agents/maestro-specialist.md` con dos secciones nuevas al final: `## 3-Retry Protocol` y `## Manual-Pass Logging`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Estructura actual de maestro-specialist.md -->
<!-- Termina con ## Key Documents y 3 bullets de archivos clave -->
<!-- Las dos secciones nuevas van AL FINAL después de ## Key Documents -->
<!-- D-09: protocolo 3 pasos — leer log, clasificar, manual-pass solo si no corregible -->
<!-- D-10: manual-pass en HTML del reporte Maestro en sección "## Manual-pass decisions" -->
<!-- El HTML de Maestro ya existe por cliente/fecha — no crear nueva infraestructura -->
<!-- El ejemplo HTML en CONTEXT.md usa <section class="manual-pass"> con <ul><li> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar sección ## 3-Retry Protocol con pasos concretos de decisión</name>
  <files>ai-specs/.agents/maestro-specialist.md</files>
  <read_first>
    - ai-specs/.agents/maestro-specialist.md — leer completo; verificar que no existe sección de retry protocol; confirmar dónde termina el archivo
    - .planning/phases/06-actionable-reports-agent-precision/06-CONTEXT.md — D-09 (3 pasos exactos: leer log, clasificar, manual-pass solo si no corregible)
  </read_first>
  <action>
Agregar al FINAL de `ai-specs/.agents/maestro-specialist.md`, después de la sección `## Key Documents`, el siguiente bloque exacto:

```markdown
## 3-Retry Protocol

Cuando un flow de Maestro falla sus 3 reintentos automáticos, seguir estos pasos en orden antes de activar manual-pass:

### Paso 1 — Leer el log del último run

```bash
# Ver el output completo del flow fallido
maestro test tests/app/flows/{NN}-{feature}.yaml --format=plain 2>&1 | tail -50
```

Identificar: ¿en qué step exacto falló? ¿cuál es el mensaje de error?

### Paso 2 — Clasificar el error

| Tipo de error | Señales | Acción |
|---------------|---------|--------|
| **Selector roto** | `No element found with id/text "{X}"`, el ID del elemento cambió en un deploy reciente | Actualizar el selector en el YAML y re-correr |
| **Error de red** | `Connection timeout`, `Unable to connect to {URL}`, staging inaccesible | Verificar `{slug}.solopide.me` disponible; si staging está caído, anotar y no activar manual-pass |
| **Estado inconsistente** | Datos de prueba en estado incorrecto (carrito con items del run anterior, usuario no reseteado) | Resetear el estado manualmente o via `launchApp` + datos frescos, re-correr |
| **Bug de la app** | Error determinista en la lógica (crash, pantalla en blanco, acción no responde), reproducible en 3 reintentos | Activar manual-pass + crear ticket Linear |

### Paso 3 — Activar manual-pass solo si no corregible en el momento

**Activar manual-pass si:**
- El error es un bug de la app (crash, lógica rota, determinista)
- El selector correcto no puede identificarse sin ver el app en vivo
- El estado del app requiere intervención manual que no puede automatizarse

**No activar manual-pass si:**
- El selector puede corregirse leyendo el YAML y el log (fix en el momento)
- Staging está caído (no es fallo del flow — anotar y esperar)
- Los datos de prueba pueden resetearse con `launchApp`

**Cuando se activa manual-pass:** registrar la razón antes de continuar (ver `## Manual-Pass Logging` abajo).
```
  </action>
  <verify>
    <automated>grep -n "## 3-Retry Protocol\|### Paso 1\|### Paso 2\|### Paso 3\|Selector roto\|Estado inconsistente" /Users/lalojimenez/qa/ai-specs/.agents/maestro-specialist.md</automated>
  </verify>
  <done>
    - `grep "## 3-Retry Protocol"` retorna exactamente 1 línea
    - `grep "### Paso 1\|### Paso 2\|### Paso 3"` retorna exactamente 3 líneas
    - `grep "Selector roto\|Error de red\|Estado inconsistente\|Bug de la app"` retorna las 4 filas de la tabla de clasificación
    - `grep "Activar manual-pass si\|No activar manual-pass si"` retorna las dos subsecciones de decisión
  </done>
</task>

<task type="auto">
  <name>Task 2: Agregar sección ## Manual-Pass Logging con template HTML y regla de registro</name>
  <files>ai-specs/.agents/maestro-specialist.md</files>
  <read_first>
    - ai-specs/.agents/maestro-specialist.md — confirmar estado tras Task 1; localizar el final del archivo
    - .planning/phases/06-actionable-reports-agent-precision/06-CONTEXT.md — D-10 (sección "## Manual-pass decisions" en el HTML existente del reporte Maestro; ejemplo HTML exacto)
  </read_first>
  <action>
Agregar DESPUÉS de la sección `## 3-Retry Protocol` (al final del archivo) el siguiente bloque exacto:

```markdown
## Manual-Pass Logging

Cuando se activa un manual-pass, registrar la decisión en el HTML del reporte Maestro existente del cliente. No hay nueva infraestructura — el HTML de Maestro ya existe en `public/app-reports/` o en el path del manifest.

### Cómo registrar

Localizar el HTML del run en `public/manifest.json` → buscar la entrada del cliente/fecha → abrir el archivo HTML referenciado.

Si el HTML no tiene una sección `## Manual-pass decisions`, agregarla antes del cierre del `<body>`:

```html
<section class="manual-pass">
  <h2>Manual-pass decisions</h2>
  <ul>
    <li>
      <strong>{NN}-{feature}.yaml</strong> — {razón concisa del manual-pass}.<br>
      Razón: {descripción del error y por qué no se puede corregir en el momento}.<br>
      Aprobado por: {nombre} | {YYYY-MM-DD}
    </li>
  </ul>
</section>
```

Si ya existe la sección, agregar un nuevo `<li>` al `<ul>` existente.

### Campos obligatorios por entrada

| Campo | Descripción | Ejemplo |
|-------|-------------|---------|
| Flow name | Nombre del archivo YAML | `05-pedido.yaml` |
| Razón | Por qué falló y por qué se activa manual-pass | `Selector cambió en build 1.4.2` |
| Descripción | Detalle del error y qué se intentó | `No se encontró id="checkout_btn", se verificó en log` |
| Aprobado por | Quién activó el manual-pass | `Eduardo` |
| Fecha | Fecha del run | `2026-04-21` |

### Regla

**El manual-pass sin registro en el HTML no es válido.** Si no hay forma de abrir el HTML (no existe el archivo), crear una entrada en `QA/{CLIENT}/{DATE}/maestro-manual-pass.md` con los mismos campos.
```
  </action>
  <verify>
    <automated>grep -n "## Manual-Pass Logging\|Manual-pass decisions\|Aprobado por\|manual-pass sin registro" /Users/lalojimenez/qa/ai-specs/.agents/maestro-specialist.md</automated>
  </verify>
  <done>
    - `grep "## Manual-Pass Logging"` retorna exactamente 1 línea
    - `grep "Manual-pass decisions"` retorna la sección HTML del template
    - `grep "Aprobado por"` retorna la línea del campo en el template HTML Y la fila de la tabla de campos obligatorios
    - `grep "manual-pass sin registro"` retorna la regla que invalida manual-passes no registrados
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → ai-specs/.agents/maestro-specialist.md | Modificación de instrucciones del agente Maestro |
| maestro-specialist → HTML del reporte | El agente escribe directamente al HTML existente de Maestro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering | maestro-specialist.md | accept | Solo texto Markdown. El protocolo mal aplicado se evidencia en el próximo run y es reversible. |
| T-06-07 | Repudiation | Manual-pass HTML logging | mitigate | La regla "manual-pass sin registro no es válido" + el template con "Aprobado por + fecha" crean traza auditable. Si el HTML no existe, fallback a .md en QA/{CLIENT}/{DATE}/. |
</threat_model>

<verification>
Después de ejecutar ambas tareas:

```bash
# 1. Verificar las dos secciones principales
grep -n "## 3-Retry Protocol\|## Manual-Pass Logging" \
  /Users/lalojimenez/qa/ai-specs/.agents/maestro-specialist.md

# 2. Verificar la tabla de clasificación de errores
grep -n "Selector roto\|Error de red\|Estado inconsistente\|Bug de la app" \
  /Users/lalojimenez/qa/ai-specs/.agents/maestro-specialist.md

# 3. Verificar template HTML manual-pass
grep -n "Manual-pass decisions\|Aprobado por\|class=\"manual-pass\"" \
  /Users/lalojimenez/qa/ai-specs/.agents/maestro-specialist.md

# 4. Verificar que el archivo mantiene sus secciones originales
grep -n "## Key Responsibilities\|## Flow Structure\|## Key Documents" \
  /Users/lalojimenez/qa/ai-specs/.agents/maestro-specialist.md
```
</verification>

<success_criteria>
1. `ai-specs/.agents/maestro-specialist.md` contiene sección `## 3-Retry Protocol` con Paso 1, Paso 2, y Paso 3
2. El Paso 2 tiene tabla de 4 tipos de error (Selector roto, Error de red, Estado inconsistente, Bug de la app) con señales y acción por tipo
3. El Paso 3 distingue explícitamente cuándo activar vs no activar manual-pass
4. Existe sección `## Manual-Pass Logging` con template HTML para `<section class="manual-pass">`
5. La tabla de campos obligatorios incluye: Flow name, Razón, Descripción, Aprobado por, Fecha
6. La regla "El manual-pass sin registro en el HTML no es válido" está documentada
7. El fallback a `QA/{CLIENT}/{DATE}/maestro-manual-pass.md` está documentado para cuando el HTML no existe
</success_criteria>

<output>
Después de completar, crear `.planning/phases/06-actionable-reports-agent-precision/06-04-SUMMARY.md`
</output>
