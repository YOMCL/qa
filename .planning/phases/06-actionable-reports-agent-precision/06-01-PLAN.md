---
phase: 06-actionable-reports-agent-precision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-specs/.commands/report-qa.md
autonomous: true
requirements:
  - PROC-05
  - PROC-06

must_haves:
  truths:
    - "El reporte generado por /report-qa muestra en sus primeras 3 líneas el veredicto, score y número de issues críticos"
    - "La sección Accionables lista solo P0 y P1 con dueño (Tech/QA/PM) y plazo relativo"
    - "Un PM o Tech Lead puede leer el estado del cliente sin hacer scroll"
  artifacts:
    - path: "ai-specs/.commands/report-qa.md"
      provides: "Template de /report-qa con resumen ejecutivo + sección Accionables"
      contains: "## Accionables"
  key_links:
    - from: "Step 6 en report-qa.md"
      to: "header del reporte generado"
      via: "instrucción de formato de las primeras 3 líneas"
      pattern: "Veredicto.*Score.*issues críticos"
    - from: "Sección Accionables"
      to: "tabla 4 columnas"
      via: "instrucción de formato Issue|Severidad|Dueño|Plazo"
      pattern: "Issue.*Severidad.*Dueño.*Plazo"
---

<objective>
Actualizar `ai-specs/.commands/report-qa.md` para que los reportes generados por `/report-qa` sean inmediatamente útiles para stakeholders no-QA.

Purpose: Un Tech Lead o PM que abre el reporte debe ver en las primeras 3 líneas si el cliente está listo para deploy, qué score tiene y cuántos issues críticos hay — sin necesidad de interpretar tablas ni leer secciones completas. La sección Accionables elimina la ambigüedad de "quién hace qué y cuándo".

Output: `ai-specs/.commands/report-qa.md` actualizado con (1) instrucción de resumen ejecutivo en las primeras 3 líneas del reporte generado, aplicado a ambos formatos (.md y .html), y (2) sección Accionables con tabla 4 columnas (Issue | Severidad | Dueño | Plazo) limitada a P0/P1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Estructura actual de report-qa.md — extraída para orientar al executor -->
<!-- Step 6 genera dos archivos: .md + .html + actualiza manifest.json -->
<!-- El ## Report Structure al final muestra el template actual con ## Summary y ## Recommendations -->
<!-- ## Recommendations ya tiene tabla Issue|Priority|Owner|Action — la sección Accionables la reemplaza/extiende con columnas específicas -->
<!-- Las instrucciones del Step 6 listan: Resumen ejecutivo: modos completados, issues por severidad, veredicto -->
<!-- El resumen ejecutivo nuevo va INMEDIATAMENTE DEBAJO del título `# QA Report: {CLIENT} — {DATE}` en AMBOS formatos -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar instrucción de resumen ejecutivo 3 líneas en Step 6 y en Report Structure</name>
  <files>ai-specs/.commands/report-qa.md</files>
  <read_first>
    - ai-specs/.commands/report-qa.md — leer completo antes de editar; identificar Step 6 y ## Report Structure
    - .planning/phases/06-actionable-reports-agent-precision/06-CONTEXT.md — D-04, D-05 (formato exacto del resumen ejecutivo)
  </read_first>
  <action>
Editar `ai-specs/.commands/report-qa.md` para agregar el resumen ejecutivo en dos lugares:

**1. Dentro del Step 6 — instrucción de generación:**

En el bloque `**a) QA/{CLIENT}/{DATE}/qa-report-{DATE}.md**`, la lista que empieza con "Include:" tiene como primer ítem "Resumen ejecutivo: modos completados, issues por severidad, veredicto". Reemplazar ese ítem por:

```
     - **Resumen ejecutivo** (primeras 3 líneas bajo el título, ANTES de cualquier sección):
       `{VEREDICTO} | Score {N}/100 | {X} issues críticos (P0: {N}, P1: {M})`
       Ejemplo: `CON_CONDICIONES | Score 72/100 | 1 issue crítico (P0: 0, P1: 1)`
       Valores posibles de veredicto: LISTO, CON_CONDICIONES, NO_APTO, BLOQUEADO
       Seguido de `---` (línea horizontal separadora)
```

En el bloque `**b) public/qa-reports/{CLIENT}-{DATE}.html**`, agregar como primer ítem de contenido:

```
     - Resumen ejecutivo en las primeras 3 líneas visibles del body: `{VEREDICTO} | Score {N}/100 | {X} issues críticos (P0: {N}, P1: {M})` — mismo contenido que el .md, en un `<div class="executive-summary">` antes del primer `<section>`
```

**2. En ## Report Structure — actualizar el template:**

Después de la línea `# QA Report: {CLIENT} — {DATE}` y antes de `## Summary`, agregar:

```markdown
{VEREDICTO} | Score {N}/100 | {X} issues críticos (P0: {N}, P1: {M})

---
```

Esto aplica al template en `## Report Structure` al final del archivo (es la referencia visual que usa Claude al generar).
  </action>
  <verify>
    <automated>grep -n "issues críticos" /Users/lalojimenez/qa/ai-specs/.commands/report-qa.md</automated>
  </verify>
  <done>El archivo contiene la instrucción del resumen ejecutivo en Step 6 (sección .md y sección .html) y el template en ## Report Structure muestra las 3 líneas bajo el título. `grep "issues críticos"` retorna al menos 3 ocurrencias (Step 6a, Step 6b, Report Structure).</done>
</task>

<task type="auto">
  <name>Task 2: Reemplazar sección Recommendations por sección Accionables con tabla 4 cols y lógica P0/P1-only</name>
  <files>ai-specs/.commands/report-qa.md</files>
  <read_first>
    - ai-specs/.commands/report-qa.md — confirmar estado tras Task 1; localizar ## Recommendations en Step 6 y en ## Report Structure
    - .planning/phases/06-actionable-reports-agent-precision/06-CONTEXT.md — D-01, D-02, D-03 (solo P0+P1, plazos relativos, tabla 4 cols)
  </read_first>
  <action>
Editar `ai-specs/.commands/report-qa.md` para agregar la sección Accionables. Hay que tocar DOS lugares:

**1. En el Step 6a (lista "Include:"):**

Localizar el ítem que menciona "Gate de Rollout con veredicto final". Antes de ese ítem, agregar:

```
     - **Accionables** (auto-generado por Claude, solo P0 y P1):
       Tabla 4 columnas: `Issue | Severidad | Dueño | Plazo`
       - Dueño: uno de `Tech`, `QA`, `PM` — asignar según tipo de issue:
         - Bug de app/API → Tech
         - Selector roto / flaky / cobertura de test → QA
         - Config de cliente, precio, dato incorrecto visible → PM
       - Plazo relativo a la fecha del run:
         - P0 → "0-1 días (urgente)"
         - P1 → "2-5 días (esta semana)"
       - P2 y P3 NO van en Accionables — continúan en sección "Issues detallados"
       Ejemplo:
       | Issue | Severidad | Dueño | Plazo |
       |-------|-----------|-------|-------|
       | Checkout falla para mobile | P1 | Tech | 2-5 días |
```

**2. En ## Report Structure (template al final del archivo):**

Localizar la sección `## Recommendations` del template y reemplazarla por:

```markdown
## Accionables

> Solo P0 y P1. P2/P3 continúan en Issues detallados.

| Issue | Severidad | Dueño | Plazo |
|-------|-----------|-------|-------|
| {descripción corta del issue} | P0/P1 | Tech/QA/PM | 0-1 días / 2-5 días |
```

La tabla existente de ## Recommendations (que tiene columnas Issue|Priority|Owner|Action) queda reemplazada por la nueva con columnas Issue|Severidad|Dueño|Plazo.
  </action>
  <verify>
    <automated>grep -n "Accionables\|Dueño\|Plazo\|0-1 días" /Users/lalojimenez/qa/ai-specs/.commands/report-qa.md</automated>
  </verify>
  <done>
    - `grep "## Accionables"` retorna al menos 2 líneas (Step 6a y Report Structure)
    - `grep "Dueño"` retorna ocurrencias en la tabla de instrucciones y en el template
    - `grep "0-1 días"` retorna la instrucción de plazo P0
    - `grep "Recommendations"` puede seguir apareciendo en Step 7 Escalation, pero NO debe aparecer en el template de Report Structure (fue reemplazada)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → ai-specs/.commands/report-qa.md | Modificación de instrucciones que Claude ejecuta al generar reportes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | report-qa.md | accept | Solo texto Markdown — no código ejecutable. Worst case: reporte mal formateado. |
| T-06-02 | Information Disclosure | Sección Accionables | accept | El dueño asignado (Tech/QA/PM) es información interna del equipo YOM. Repo privado. |
</threat_model>

<verification>
Después de ejecutar ambas tareas:

```bash
# 1. Verificar que las 3 líneas del resumen ejecutivo están en el template
grep -A3 "QA Report: {CLIENT}" /Users/lalojimenez/qa/ai-specs/.commands/report-qa.md

# 2. Verificar que Accionables reemplazó Recommendations en Report Structure
grep -n "## Accionables\|## Recommendations" /Users/lalojimenez/qa/ai-specs/.commands/report-qa.md

# 3. Verificar columnas correctas
grep "Issue.*Severidad.*Dueño.*Plazo" /Users/lalojimenez/qa/ai-specs/.commands/report-qa.md

# 4. Verificar plazos relativos documentados
grep "0-1 días\|2-5 días" /Users/lalojimenez/qa/ai-specs/.commands/report-qa.md
```
</verification>

<success_criteria>
1. `ai-specs/.commands/report-qa.md` contiene la instrucción de resumen ejecutivo en Step 6 para ambos formatos (.md y .html)
2. El template en `## Report Structure` muestra `{VEREDICTO} | Score {N}/100 | {X} issues críticos (P0: {N}, P1: {M})` como primeras líneas bajo el título
3. Existe sección `## Accionables` con tabla `Issue | Severidad | Dueño | Plazo` en Step 6 y en el template
4. La instrucción especifica explícitamente que solo P0 y P1 entran en Accionables
5. Los plazos relativos P0 → "0-1 días (urgente)" y P1 → "2-5 días (esta semana)" están documentados
6. Los dueños posibles (Tech / QA / PM) están documentados con criterio de asignación
</success_criteria>

<output>
Después de completar, crear `.planning/phases/06-actionable-reports-agent-precision/06-01-SUMMARY.md`
</output>
