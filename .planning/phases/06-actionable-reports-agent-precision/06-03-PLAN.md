---
phase: 06-actionable-reports-agent-precision
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ai-specs/.commands/triage-playwright.md
autonomous: true
requirements:
  - AGENT-03

must_haves:
  truths:
    - "Durante el triage, Claude clasifica un timeout según su duración sin tener que improvisar el criterio"
    - "La rúbrica <5s/5-30s/>30s está en el mismo archivo que usa Claude al triagear"
  artifacts:
    - path: "ai-specs/.commands/triage-playwright.md"
      provides: "Rúbrica de timeout integrada en el flujo de triage Step 4"
      contains: "5s.*selector\|30s.*red/staging\|30s.*bug"
  key_links:
    - from: "triage-playwright.md Step 4 bloque ambiente"
      to: "clasificación por duración de timeout"
      via: "rúbrica <5s/5-30s/>30s"
      pattern: "<5s.*selector"
---

<objective>
Agregar la rúbrica de clasificación de timeouts por duración al bloque `ambiente` del Step 4 de `ai-specs/.commands/triage-playwright.md`.

Purpose: Actualmente el triage trata todos los timeouts como "ambiente" sin distinguir si es un selector roto (corregible en el test), staging caído (ignorar este run), o un bug de la app (escalar). La rúbrica convierte una decisión subjetiva en un criterio reproducible basado en dato observable (duración del timeout).

Output: `ai-specs/.commands/triage-playwright.md` con la rúbrica de timeout integrada en el bloque de decisión para categoría `ambiente` en Step 4, y también en la sección `## Reglas` al final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Estructura actual de triage-playwright.md -->
<!-- Step 4 tiene bloque "Si `ambiente` (owner: qa):" con opciones A/B/C -->
<!-- "Si hay más de 5 fallos → priorizar los de categoría `bug` primero" en Reglas -->
<!-- La rúbrica va DENTRO del bloque ambiente de Step 4 (antes de la línea "- Leer el spec afectado") -->
<!-- Y también se agrega en ## Reglas como bullet de referencia rápida -->
<!-- Patrón del archivo: bloques de decisión con opciones A/B/C + sección Reglas al final -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insertar rúbrica de timeout en Step 4 bloque ambiente y en sección Reglas</name>
  <files>ai-specs/.commands/triage-playwright.md</files>
  <read_first>
    - ai-specs/.commands/triage-playwright.md — leer completo; localizar Step 4 bloque "Si `ambiente`" y sección ## Reglas
    - .planning/phases/06-actionable-reports-agent-precision/06-CONTEXT.md — D-08 (valores exactos: <5s, 5-30s, >30s y sus clasificaciones)
  </read_first>
  <action>
Editar `ai-specs/.commands/triage-playwright.md` en DOS lugares:

**Lugar 1 — Step 4, bloque `ambiente`:**

Localizar el bloque que empieza con `**Si `ambiente` (owner: qa):**`. Inmediatamente después de esa línea y ANTES de `- Leer el spec afectado`, agregar:

```markdown
- **Clasificar el timeout por duración** (leer la duración del error en el output de Playwright):
  - `<5s` → **selector issue**: el elemento no existe o cambió (bug de UI o deploy reciente) — reclasificar como `bug` si es reproducible
  - `5-30s` → **red/staging**: staging lento o caído — ignorar para este run, verificar `{slug}.solopide.me`
  - `>30s` → **bug o infinite loop**: lógica de la app, no infraestructura — escalar como `bug`
```

**Lugar 2 — sección ## Reglas:**

Localizar la primera línea de `## Reglas` y agregar como primer bullet (antes de "Máximo 1 ticket por grupo de fallo"):

```markdown
- **Rúbrica de timeout por duración**: `<5s` = selector issue, `5-30s` = red/staging (ambiente), `>30s` = bug o infinite loop — usar siempre al triagear categoría `ambiente`
```
  </action>
  <verify>
    <automated>grep -n "selector issue\|red/staging\|infinite loop\|Rúbrica de timeout" /Users/lalojimenez/qa/ai-specs/.commands/triage-playwright.md</automated>
  </verify>
  <done>
    - `grep "selector issue"` retorna al menos 2 líneas (Step 4 y Reglas)
    - `grep "5-30s"` retorna la línea con la clasificación de red/staging
    - `grep "Rúbrica de timeout"` retorna la línea en la sección Reglas
    - `grep "infinite loop"` retorna la clasificación del timeout >30s
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude → ai-specs/.commands/triage-playwright.md | Modificación de instrucciones de triage; afecta cómo se clasifican fallos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | triage-playwright.md | accept | Texto Markdown leído por Claude, no ejecutado. Un criterio incorrecto se evidencia en el próximo triage y se puede revertir. |
</threat_model>

<verification>
```bash
# 1. Verificar que la rúbrica está en ambos lugares
grep -n "selector issue\|5-30s\|infinite loop\|Rúbrica de timeout" \
  /Users/lalojimenez/qa/ai-specs/.commands/triage-playwright.md

# 2. Verificar posición correcta en Step 4 (debe aparecer cerca del bloque ambiente)
grep -n -B2 -A6 "selector issue" \
  /Users/lalojimenez/qa/ai-specs/.commands/triage-playwright.md

# 3. Verificar que el archivo mantiene sus secciones originales intactas
grep -n "### 1\. Adoptar\|### 2\. Cargar\|### 3\. Mostrar\|### 4\. Triagear\|### 5\. Persistir\|### 6\. Resumen" \
  /Users/lalojimenez/qa/ai-specs/.commands/triage-playwright.md
```
</verification>

<success_criteria>
1. La rúbrica `<5s / 5-30s / >30s` aparece dentro del bloque `ambiente` del Step 4, antes de la instrucción "Leer el spec afectado"
2. La rúbrica también aparece como bullet en la sección `## Reglas` para referencia rápida
3. Los tres valores están correctamente mapeados: `<5s` = selector issue, `5-30s` = red/staging, `>30s` = bug o infinite loop
4. El archivo mantiene su estructura original (todos los Steps del 1 al 6, Reglas y Archivos clave intactos)
</success_criteria>

<output>
Después de completar, crear `.planning/phases/06-actionable-reports-agent-precision/06-03-SUMMARY.md`
</output>
